# PolicyCaps red-team: moonshotai/kimi-k3

_finish=length seconds=97.4 usage={'prompt_tokens': 12385, 'completion_tokens': 16000, 'total_tokens': 28385, 'cost': 0.2769822, 'is_byok': False, 'prompt_tokens_details': {'cached_tokens': 64, 'cache_write_tokens': 0, 'audio_tokens': 0, 'video_tokens': 0}, 'cost_details': {'upstream_inference_cost': 0.2769822, 'upstream_inference_prompt_cost': 0.0369822, 'upstream_inference_completions_cost': 0.24}, 'completion_tokens_details': {'reasoning_tokens': 17872, 'image_tokens': 0, 'audio_tokens': 0}}_

Let me carefully analyze this design review request. The task is to adversarially review the DESIGN for "signed PolicyCaps" — binding a governed worker's authority (capabilities + leash caps) to an HMAC-signed grant the host verifies each action.

Let me trace through the design and the seam code carefully to find real issues.

**The design's core claims:**
1. Every governed action verifies the grant and sources capabilities from VERIFIED caps (not mutable `session.capabilities`)
2. Leash capped at `stricter(host_leash, leash_cap)`
3. Fail-closed: tamper/absent-key/wrong-subject/malformed → zero caps + strictest leash
4. Backward compatible: no PolicyCaps → legacy behavior
5. Honest scope: symmetric HMAC, single trust domain, NOT a hard boundary against in-process re-signer

**Attack surface 1: BYPASS / INCOMPLETE MEDIATION**

Let me look at the seam code. The design says `governance.py` will be modified: "govern_action + reauthorized_or_denied source caps from the verified grant and cap the leash."

Current code in `govern_action`:
```python
policy = issue_policy(
    "collab-policy", action_id, tuple(session.capabilities),
    ...
)
```

And in `reauthorized_or_denied`:
```python
policy = issue_policy(
    "collab-policy", action_id, tuple(session.capabilities),
    ...
)
```

Both read `session.capabilities` today. The design says both will source from verified grant. OK, the design names both call sites. Good.

But wait — the leash. In `govern_action`:
```python
leash = _resolve_leash(session, tool, leash)
```
This resolves the leash from host config/override. The design says: "resolves the leash as today, then caps it: `effective = stricter(host_leash, leash_cap)`."

Now, the critical question: does the leash cap apply on BOTH the act path AND the approve/re-gate path?

Look at `execute_and_verify`:
```python
def execute_and_verify(session, tool: Tool, directive, action_id: str, args: dict) -> Decision:
    leash = _leash_for(session, tool)
```

This re-resolves the leash from `_leash_for(session, tool)` — which reads `session.leash_overrides` — NOT the capped leash! This is called:
1. From `govern_action` when `leash == ACT_THEN_REPORT` (act path)
2. From `approve_proposal` (presumably, via the approve path — let me check)

The approve path: `approve(session, ledger, proposal)` → `approve_proposal(session, proposal)`. We don't see `approve_proposal` in the material, but `reauthorized_or_denied` is the re-gate, and `execute_and_verify` is described as "Used both for act_then_report and for an approved propose_first action."

So here's a REAL issue: `execute_and_verify` re-derives `leash = _leash_for(session, tool)` from mutable session config, ignoring any cap. Now, does the leash value matter for authority in `execute_and_verify`? Looking at the function, `leash` is only used for the Decision record (`leash=leash` in the Decision). The actual execution happens regardless — by the time `execute_and_verify` is called, the decision to run has been made. So the leash in `execute_and_verify` is mostly cosmetic/audit.

BUT — the important question is the approve path. When a held action is approved:
- `reauthorized_or_denied` re-checks capability (design says it will source from verified caps — good)
- But does the approve path re-check the LEASH against the cap? 

Scenario: Grant caps `run_command` at `propose_first`. At origination, `govern_action` computes `effective = stricter(host_leash, cap)` = `propose_first` → HELD. Good. Now the host approves. `approve_proposal` calls `reauthorized_or_denied` (re-checks capability — fine) then `execute_and_verify`. The leash cap was checked at origination, but what if `set_leash(run_command, act_then_report)` is called AFTER origination but BEFORE approval? The approval path runs the action — but that's fine, because approval is the host's explicit act. The leash `propose_first` means "hold for approval" — once approved, running is correct.

Hmm, but wait — there's a subtler issue. What about the leash cap check on the approve path for a DIFFERENT reason: the design says "the leash cap applies on BOTH the act path and the approve/re-gate path." Let me think about whether the re-gate needs the leash cap.

Actually the more important issue: in `govern_action`, the leash is resolved BEFORE the capability gate:
```python
leash = _resolve_leash(session, tool, leash)
```
Then the capability gate. Then the leash branches. The design says the cap is applied after resolving. Where exactly? "resolves the leash as today, then caps it: effective = stricter(host_leash, leash_cap)."

But `execute_and_verify` re-derives the leash via `_leash_for(session, tool)` — the UNCAPPED leash — and records it in the Decision. So the Decision's `leash` field could show `act_then_report` even when the cap is `propose_first`... wait no, because if the cap is `propose_first`, `govern_action` would have HELD, not called `execute_and_verify`. Unless the host override `leash=ACT_THEN_REPORT` is passed and the cap... hmm.

Actually wait. Let me re-read. In `govern_action`, if the capped effective leash is `act_then_report` (cap allows it, host says act), then `execute_and_verify` is called, and it re-derives `leash = _leash_for(session, tool)`. If the host override was passed as the `leash` kwarg (per-task), `_leash_for` doesn't know about it — it reads `session.leash_overrides`. So there's ALREADY a discrepancy in the existing code between the leash used for the decision and the leash recorded. But that's a pre-existing cosmetic issue, not a PolicyCaps hole.

The REAL hole for PolicyCaps: **the design must ensure the cap is applied at every point where the leash is resolved for an execution decision.** The design says `govern_action` caps it. But what about `approve_proposal`? We don't see it, but the re-gate `reauthorized_or_denied` only re-checks capability and workspace, NOT the leash. Is that a hole?

Scenario: grant caps `run_command` at `notify_only` (strictest). Host leash is `act_then_report`. Effective = `notify_only` → NOTIFIED, not run. Fine. Now... can the action ever run? Only via approval of a held action. But it was NOTIFIED, not HELD — is a notified action approvable? Probably not through the proposal channel. OK.

Scenario: cap is `propose_first`. Action HELD. Host approves → runs. That's the intended semantics of `propose_first`. Fine.

So the leash cap on the re-gate path is arguably not needed IF the cap is enforced at origination and approval is a host act. But the review prompt specifically asks: "Does the leash cap apply on BOTH the act path and the approve/re-gate path?" Let me think about whether there's a real bypass.

Hmm — here's one. `execute_and_verify` reads `_leash_for(session, tool)` fresh. Suppose the design naively caps the leash only in `govern_action`'s local `leash` variable. The approve path (`approve_proposal` → `execute_and_verify`) never consults the cap. Is there a scenario where this matters for authority? The leash in `execute_and_verify` doesn't gate execution — execution already happened by decision of the caller. So the cap's enforcement point is the branch in `govern_action`. As long as `approve_proposal` doesn't independently resolve the leash and decide to run based on it... we can't see `approve_proposal`. This is a gap in the material — the design doesn't show how the approve path applies the cap. Worth flagging as a mediation-completeness concern: the design names `reauthorized_or_denied` for capability re-gating but says nothing about leash-cap enforcement on the approve path, and `execute_and_verify` re-derives the uncapped leash from mutable config.

Actually, let me reconsider. There IS a real authority-relevant concern: the design says "every governed action verifies the grant." The approve path runs an action. Does `approve_proposal` verify the grant? `reauthorized_or_denied` will (per the design) source caps from the verified grant. But the leash cap — if the host calls `set_leash(run_command, act_then_report)` and then approves a held proposal, the action runs. Is that a violation? No — approval is the host's explicit authorization; `propose_first` means "run only after approval." The cap `propose_first` was satisfied: the action was held and approved.

But here's a nastier one: what if the cap is `notify_only` — "never run, only surface"? Then `govern_action` returns NOTIFIED. A notified action never enters the proposal/approval flow (it's not HELD). So it can never run. Good — UNLESS the approve path can be reached some other way. `approve_proposal` operates on a `Proposal` from the propose channel (Step 1). Proposals are surfaced by the proposer. If a proposal for `run_command` is surfaced and approved, does it go through `govern_action` again or straight to `reauthorized_or_denied` + `execute_and_verify`? From the view code: `approve` → `approve_proposal(session, proposal)`. The proposal carries `p.decision` (a HELD decision from origination). If `approve_proposal` calls `reauthorized_or_denied` then `execute_and_verify`, the leash cap is never re-consulted. So: **a tool capped at `notify_only` could still RUN via the proposal-approve path**, because the cap is only enforced in `govern_action`'s leash branch, and the approve path bypasses that branch entirely.

Wait, but would a proposal exist for a `notify_only`-capped tool? The propose channel (Step 1) surfaces proposals — we don't see `propose.py`. If the proposer calls `govern_action` with the cap applied, a `notify_only` cap yields NOTIFIED, not HELD, so no proposal to approve. But if the proposer creates proposals independently of the capped leash (e.g., it uses the uncapped leash or its own logic), then approval runs it. This is genuinely ambiguous in the material — a real finding: **the design does not specify cap enforcement on the approve path, and `execute_and_verify` re-derives the leash from mutable `session.leash_overrides`, so a `notify_only` cap (which must mean "never run") may be unenforced if any path reaches `execute_and_verify` without passing through `govern_action`'s capped branch.** The fix: apply the cap inside `execute_and_verify` (or in `reauthorized_or_denied`), i.e., the cap must be enforced at the moment of use, not just at origination — same TOCTOU logic the design already applies to capabilities (panel F1).

This is a strong finding. The design's own TOCTOU rationale ("a held decision may have sat while the session's capabilities changed") applies equally to leash caps: a held decision may have sat while... well, the cap can't change (it's signed), but the enforcement point is origination-only. Actually more precisely: the cap is immutable (signed), so there's no TOCTOU on the cap itself. The issue is mediation completeness: the approve path must also enforce `notify_only` caps. Let me rate this HIGH.

**Attack surface 1b: capability check reads verified caps?**

The design says both `govern_action` and `reauthorized_or_denied` will source from the verified grant. The current code reads `tuple(session.capabilities)` in both. The design names both. But — is there any OTHER place capabilities are read? 

`view.py` `snapshot()`: `"capabilities": list(getattr(session, "capabilities", ()))` — display only, not authority. But it could MISLEAD: the view shows the mutable tuple, not the verified caps. So the host sees `shell.exec` in the capabilities list (because an attacker/bug set `session.capabilities`) while the actual gate denies. That's an honesty/display bug: the view would show authority the grant doesn't confer. MEDIUM/LOW — the view should display the verified caps. Actually this matters for the "host can put a hand on it" trust: the control surface displays mutable config as if it were authority. Worth a LOW/MEDIUM finding.

Also `issue_policy` in `govern_action` — the design says "sources the capability set from the verified caps (not the mutable tuple), then feeds that to the existing core issue_policy/interpret capability gate." OK.

But here's a subtle one: **where is verification done relative to the pause check and signal emission?** In `govern_action`, `_emit_signals` is called inside the try block after `issue_policy`. The design says "every governed action: 1. verifies the grant..." — order matters. If verification happens inside the try, an exception from verify must fail closed. The design says invalid → fail closed. Fine, as long as verify() itself never raises into a pass. The design says `granted_capabilities(session)` returns zero caps on failure. Need to ensure `granted_capabilities` catches all exceptions from `verify()` and returns `()`. The design lists `verify()` and `granted_capabilities(session)` as separate functions — the wiring must ensure `granted_capabilities` treats verify-failure as `()`, not as "fall back to session.capabilities." That's the classic fail-open trap. The design SAYS fail closed, but the proof only covers tamper, not "verify() raises." Since this is a design review, I should demand the fail-closed contract be explicit: `granted_capabilities` must be total (never raise, never fall back to the mutable tuple when caps are present-but-invalid).

**The absent-key case:** "Absent key when a grant is present → fail closed." Good. But what about absent GRANT? "Backward compatible: no PolicyCaps → legacy." So `granted_capabilities(session)` when `session.policy_caps is None` returns `session.capabilities` (legacy). When `session.policy_caps` is present but `session.caps_key` is None → zero caps. OK, that's the stated behavior. The hole: **how does `granted_capabilities` distinguish "no caps configured (legacy)" from "caps present but key absent (fail closed)"?** It must check `session.policy_caps is not None` FIRST. If someone sets `policy_caps` but forgets `caps_key`, fail closed — good, that's stated. But what if `policy_caps` is present and malformed (not a SignedPolicyCaps, e.g., a dict the attacker injected)? Malformed → fail closed, stated. OK.

**The real backward-compat hole (attack surface 6):** "no PolicyCaps ⇒ legacy behaviour." An attacker who can mutate `session.capabilities` (the realistic threat the design names: "a bug or an injected config flip in a different component") can ALSO simply... not attach caps. But wait — attaching caps is a host/ops decision at session construction. If the host provisions caps, the session has them. The attacker's leverage is mutating config, not choosing whether caps exist. So the legacy path is only a bypass if the attacker can DELETE `session.policy_caps` from the session. Can they? If the threat is "a bug or injected value mutates session config," then `session.policy_caps = None` is exactly such a mutation — and it silently reverts to legacy, where `session.capabilities` is authoritative and mutable! **This is a CRITICAL/HIGH finding: deleting the grant (or setting it to None) is itself a config mutation that fails OPEN to the legacy path.** The design's fail-closed covers tamper-with-caps, absent-key, wrong-subject, malformed — but NOT "caps attribute removed/None'd." The mitigation: the session should record "caps were provisioned" separately (e.g., a flag set at construction, or the host requiring caps), so that "expected caps but none present" fails closed. The design says "Invalid/absent-when-required ⇒ fail closed" — "absent-when-required"! So the design DOES gesture at this: absent-when-required fails closed. But it doesn't define how "required" is determined. If "required" is a host-set boolean `require_policy_caps`, then an attacker mutating config could flip THAT too... but that's the same mutable-config problem. The honest answer: in a single trust domain where the attacker can mutate arbitrary session attributes, NO attribute is safe — the attacker could set `session.policy_caps = None` OR flip a `require` flag. Hmm. But the design's stated threat is narrower: "a non-crypto mutation of capabilities or a leash-loosen by the control surface." The control surface (`view.py`) only has `set_leash`, `set_proactivity`, `pause`, `resume` — it can't touch `policy_caps`. So the realistic threat (control surface + injected config values) — can the control surface delete caps? `set_leash` writes `session.leash_overrides`. Not caps. So the caps-deletion attack requires arbitrary attribute write, which is the "fully in-process adversarial component" the design explicitly scopes out... mostly. Actually no — "an injected config flip in a different component that doesn't go through mint" — a config flip could include `session.policy_caps = None`. I think this deserves a HIGH finding with the fix: construction-time binding — if caps are provided at construction, store them in a way the legacy path can't silently substitute; specifically, `granted_capabilities` should treat "session was built with caps" as sticky (e.g., a private `_caps_required` flag set only by the constructor, and document that the legacy path exists only when caps were never provisioned). And the doc should NAME this: "absent-when-required" needs a definition of "required" that isn't itself trivially mutable, or at least acknowledge that stripping the grant is the same class of mutation as editing it and must fail closed.

Actually, let me reconsider severity. The design says: "verifies the grant... Invalid/absent-when-required ⇒ fail closed." And property: "Absent key when a grant is present, wrong subject, malformed caps → same [fail closed]." Note: "absent KEY when a grant is present" — but not "absent GRANT." The backward-compat property says no PolicyCaps → legacy. So the spec as written: grant absent → legacy (open). The word "absent-when-required" appears in the verify step but "required" is never defined. This is a genuine spec gap: **grant-stripping fails open.** For opt-in hardening this is partly inherent (legacy must exist for compat), but the design should (a) define "required" — e.g., a session constructed with `policy_caps` records that fact immutably-ish, and (b) the proof should include "strip the grant → denied," not just "flip a capability → denied." I'll call it HIGH.

**Attack surface 2: WIDEN**

- Capability add via `session.capabilities`: covered IF all gates read verified caps. Both named call sites. OK in principle.
- Leash loosen via `set_leash`/`leash_overrides` below the cap: the cap is applied in `govern_action` as `stricter(host_leash, leash_cap)`. `set_leash` writes `leash_overrides`; `_resolve_leash` reads it; then cap applied. So `set_leash(run_command, act_then_report)` with cap `propose_first` → effective `propose_first`. Good — IF the cap is applied to the OVERRIDE too, including the `leash` kwarg override in `govern_action`. The design says "resolves the leash as today, then caps it" — "as today" includes the host per-task `leash` kwarg. So the kwarg is capped too. Good. But note the docstring in `govern_action`: "A host leash may tighten OR loosen relative to the tool default (host authority over their own leash)" — with caps, loosening is bounded by the cap. The design should update this docstring, else it overstates host authority. Minor doc fix.

- **`stricter` rank ordering off-by-one:** The design gives:
```
act_then_report (0, loosest) < propose_first (1) < notify_only (2, strictest)
effective_rank = max(host_rank, cap_rank)
```
max = stricter. cap=propose_first(1), host=act_then_report(0) → max=1 = propose_first. Correct. cap=propose_first(1), host=notify_only(2) → 2 = notify_only (host tightens). Correct. No off-by-one in the stated formula — `max` over the rank with strictest=highest is right. The review prompt asks specifically: "no off-by-one in the rank ordering that lets act_then_report through a propose_first cap." With max(0,1)=1, act_then_report does NOT get through. Correct as designed. I should verify the mapping direction is the risk: if someone implemented `min` instead of `max`, or ranked strictest=0, it would fail open. The design is correct; the implementation must use max with this exact ordering. Also: what about a tool with NO entry in `leash_caps`? `leash_cap(session, tool)` returns... what? If a tool isn't named in the grant's `leash_caps`, is the cap absent (host leash governs — legacy) or strictest (fail closed)? The design doesn't say! **Missing per-tool cap default is unspecified.** Two readings: (a) no cap for that tool → host leash governs (fail open relative to the grant — but the grant didn't speak, so arguably fine); (b) strictest. For a grant that lists capabilities but forgets `run_command`'s leash cap, reading (a) means `run_command` (if capability granted) runs at the tool default `propose_first`... which is its default anyway. Hmm, but the principle "the grant is the complete statement of authority" suggests absent cap = strictest (notify_only) or at least the tool default, whichever is stricter. Actually the safest: absent cap → strictest (notify_only), because the grant is the authority's complete statement; silence should not confer looseness. But that could break the "tighten still works" story... no, tighten within grant. I'd flag: **unspecified default for tools missing from leash_caps — must default to strictest (or at minimum tool-default intersected), and be documented.** MEDIUM. Similarly, capabilities: the grant's capability list is exhaustive (good — sourcing from verified caps is inherently exhaustive). But leash_caps silence is ambiguous.

Also: what about a capability in the grant that doesn't correspond to any tool, or `leash_caps` for a tool whose capability isn't granted? Harmless. And `leash_caps` with an unknown tool name — ignore or fail closed? Malformed caps → fail closed is stated; unknown tool in caps is arguably malformed. Minor.

**None fields:** `issuer: str, subject: str` — what if `subject` is None or empty? Verify must reject None/empty subject (a grant with `subject: ""` or None must not match). And `capabilities: None`? Malformed → fail closed. The design says malformed → fail closed; the canonicalization must handle types strictly. I'll fold this into the canonicalization finding.

**Attack surface 3: FAIL-CLOSED**

- Tamper: covered by HMAC. Proof #3.
- Absent key with caps present: covered (stated).
- Wrong subject: covered (proof #4).
- Malformed/empty caps: stated. Empty capabilities tuple in a VALIDLY SIGNED grant → zero capabilities, strictest leash? A validly-signed grant with `capabilities: ()` means the authority granted nothing — that's fail-closed by construction, fine. But `leash_caps: {}` validly signed → per-tool default question above.
- **verify() exception treated as pass:** the design must specify `granted_capabilities` is total. The seam's existing pattern: `except Exception → DENIED`. If `granted_capabilities` raises inside the `try` in `govern_action`, the existing `except Exception` catches → DENIED. Good — actually the existing seam structure helps here. But `reauthorized_or_denied` also has try/except → DENIED. So if the wiring puts verification inside those try blocks, exceptions deny. But `leash_cap(session, tool)` — where is it called? In the leash-resolution, which in `govern_action` happens BEFORE the try block (`leash = _resolve_leash(...)` is before the try). If `leash_cap` or `apply_cap` raises there, `govern_action` raises — violating "Never raises." The design must ensure cap application is total or wrapped. MEDIUM/LOW — implementation note: `apply_cap`/`leash_cap` must be total (unknown rank → strictest), and the capped resolution must fail closed on any error.

- **The `verify()`-returns-falsy-but-not-exception path:** fine.

- **Subject comparison:** `caps.subject == this session's workspace`. Workspace is `Path(workspace)` — NOT resolved! `Session.__init__`: `self.workspace = Path(workspace)`. No `.resolve()`. So the subject binding compares against an UNRESOLVED path. **Symlink/relative-path attack on the subject binding:** a grant minted for `/home/user/project` presented on a session whose workspace is `/home/user/project/../project` or a symlinked path — string/Path equality fails → the grant is REJECTED (fail closed, annoying but safe). The dangerous direction: could TWO DIFFERENT workspaces compare equal? `Path("a/b") == Path("a/b")` — same string. Different strings that resolve to the same dir → NOT equal → reject (safe). Same string, different actual dirs? Only via chroot/namespace games — out of scope. What about `Path("/x") == Path("/x/")`? Path normalizes trailing slashes, so equal. Relative vs absolute: `Path("proj") != Path("/home/user/proj")` — a grant minted with subject "proj" (relative) would verify on a session with workspace "proj" — and ALSO on a session with workspace "proj" in a DIFFERENT cwd! **If the subject is a relative path, the same grant verifies on any session whose (unresolved) workspace string matches, regardless of the actual directory.** So the subject binding is only as sound as the path canonicalization. Fix: subject must be the RESOLVED absolute path (`Path(workspace).resolve()` at both mint and verify), and the design should say so. Also case-insensitive filesystems (macOS/Windows): `/Users/A` vs `/Users/a` — resolve() doesn't normalize case; two paths differing in case could be the same dir but compare unequal (fail closed — safe) or... could a grant for `/Users/A` be replayed onto a session at `/users/a` pointing to the same dir? That's not a cross-workspace replay, it's the same workspace. The cross-workspace replay with case: grant for `/tmp/A`, session workspace `/tmp/a` where `/tmp/a` is a DIFFERENT directory — on a case-insensitive FS they might be the same; on case-sensitive they differ and string-compare unequal → reject (safe). The real risk is only relative/symlinked subjects. MEDIUM finding: **subject must be canonicalized (resolve()) at mint and verify; the design says "subject binds it to ONE workspace" but Session stores the path unresolved, and the design never specifies the canonical form.** Also `os.path.realpath` vs resolve — symlinks: if workspace is reached via a symlink, minting against the symlink path and verifying against the resolved path (or vice versa) breaks; and two different symlink paths to the same dir would be different subjects — meaning a grant for the real path presented via a symlinked session path fails (safe) — but the design should just mandate resolved-absolute.

Wait, actually there's a worse replay angle: the subject is compared to "this session's workspace." But nothing binds the grant to the POLICY KEY or the SESSION beyond subject. If two workspaces share... no, subject covers it. What about replaying a grant from workspace A onto workspace B where B's workspace string equals A's subject? That requires B's workspace to BE A's path — then it IS workspace A. Fine.

**Nonce/expiry:** the design defers it (doesn't even mention it, actually — the review prompt asks "Any nonce/expiry needed for v0, or is it honestly deferred?"). The design doesn't mention expiry at all. Is that a hole? A grant, once minted, is valid forever for that workspace. Revocation: to revoke, the host... removes the caps from the session (but that's the stripping issue) or rotates `caps_key`. Key rotation revokes all grants. For v0 single-domain, no expiry is defensible but should be STATED — the design is silent on revocation/expiry. An old grant for a workspace, if the host later runs a session on the same path with the same key, is still valid — is that OK? Arguably yes (same authority, same workspace). But a STOLEN grant (exfiltrated SignedPolicyCaps blob) replays onto any session with the same workspace path and key. Within a single trust domain, the key is the root anyway. I'd say: honestly deferred is acceptable, but the doc should NAME revocation/expiry as deferred (it currently doesn't mention them at all — a small honesty gap). LOW.

**Attack surface 4: HONESTY OF THE CLAIM**

The design's honest-scope section is quite good: names symmetric HMAC, single trust domain, in-process re-signer out of scope, asymmetric/separate-process as future, ADR 0002 consistency. Does it oversell anywhere?

- Title: "Policy authorizes made cryptographic." Slightly grand but OK.
- "the config and the Step-2 control surface can only ever operate within the signed grant" — TRUE only if mediation is complete (see approve-path finding) and if grant-stripping is handled. So this sentence overstates given the stripping hole.
- "Nothing binds 'this worker was granted exactly these capabilities...'" — fine.
- The proof section: "No widening (capability): set session.capabilities = (…, 'shell.exec') by hand → run_command is still DENIED." Good. But there's no proof for "delete session.policy_caps → still denied" or "set session.caps_key = None → denied" (absent key is claimed in properties but not in the proof list; proof #3 is tamper only). The proof list should add: strip-grant, absent-key, malformed-caps, and the approve-path cap enforcement. Also no proof that `reauthorized_or_denied` uses verified caps (the re-gate widening path: set session.capabilities after origination, then approve — the re-gate must deny). Actually proof #1 covers govern_action; a proof that approval re-gates against the grant (mutate caps between hold and approve → denied) is the TOCTOU case and should be listed. MEDIUM/LOW (proof completeness).
- "Provenance. The grant carries an issuer; an audit can confirm authority, not conjure it." — `issuer` is a string inside the signed blob, so it can't be forged without the key. OK, honest. But "an audit can confirm authority" — confirm that whoever minted knew the key and claimed issuer string. Within single domain, fine.
- One oversell: "tamper-evidence" — HMAC over the caps detects edits, yes. But "tamper-evident" usually implies evidence left behind; here it's tamper-REJECTING (fail closed). Fine, minor terminology.
- The phrase in the gap section: "A misbehaving or compromised control surface could loosen run_command from propose_first to act_then_report and auto-run shell commands." With ③, the control surface (set_leash) is capped. True IF cap applied on all paths. OK.
- Does the doc imply a hard boundary anywhere? "Nothing binds..." → "That binding is what ③ adds." Then honest scope walks it back. I think the doc is mostly honest. One more: "the realistic threat... is stopped" — the realistic threat list omits grant-stripping. And the backward-compat property ("A session with no PolicyCaps behaves exactly as today") sits next to the fail-closed property without acknowledging the tension: **the same doc that says "absent → fail closed" also says "absent → legacy," and the disambiguator ("when required") is undefined.** That's the honesty gap to name.

**Canonicalization:** `signature: HMAC-SHA256(canonical(caps), caps_key)`. The design says "canonical(caps)" but never defines it! The review prompt asks: "Is the canonicalization collision/ambiguity-safe (e.g., JSON with sorted keys, stable types), or could two different caps sign the same?" The design is SILENT on the canonical form. Risks if implemented naively:
- `repr(caps)` or `str(dict)` — Python dict ordering is insertion-ordered, so `{"a":1,"b":2}` vs `{"b":2,"a":1}` produce different strings — that's fail-closed (signature mismatch), not a collision. The dangerous direction is two DIFFERENT caps canonicalizing to the SAME bytes: e.g., capabilities `("a","b")` vs `("a,b",)` if joined by commas; or tuple vs list ambiguity; or `1` vs `"1"`; or a leash_caps dict `{"run_command": "propose_first"}` vs `{"run_command:propose_first": ...}` under naive string concat. Also separators inside values.
- Also: does the signature cover the VERSION of the canonicalization scheme? Not needed for v0.
The fix: specify canonical JSON — `json.dumps(caps, sort_keys=True, separators=(",", ":"))` with a strict schema (capabilities as sorted list of strings, leash_caps as object of string→enum, issuer/subject as strings), reject unknown fields, and type-check before signing/verifying. The design must name this; "canonical(caps)" is doing a lot of unstated work. MEDIUM (design gap, easily fixed, but exactly the kind of thing that becomes a real bug: two different caps, same signature → the signature no longer binds the caps).

Actually — could two different caps sign the same be exploited? An attacker without the key can't re-sign, so a collision helps only if they can get the authority to sign cap-set A and then present cap-set B with A's signature because canonical(A)==canonical(B). E.g., authority signs `capabilities: ["fs.read:project", "fs.write:project"]`; if canonicalization is naive string join, attacker presents `capabilities: ["fs.read:project,fs.write:project"]`... which wouldn't grant anything useful (that string isn't a real capability). The exploitable direction needs a collision where B grants MORE. With naive concat: A = `("shell.exec",)` canonicalizes to "shell.exec"; B = `("shell", "exec")`? canonicalizes to "shell,exec" — different. To widen, B must be a superset that canonicalizes same as A — hard with joins, easier with dict-ordering or type confusion: A = `{"caps": ["a"]}` vs B = `{"caps": ["a"], "x": ...}` where unknown fields are dropped by canonicalization — **if canonical() only serializes known fields, an attacker can ADD unsigned fields**... but added fields don't affect the gate (gate reads known fields). The real danger: canonical() dropping a field that DOES affect authority — e.g., if `leash_caps` isn't included in the signed payload! Let me check: `SignedPolicyCaps = { caps: PolicyCaps, signature: HMAC(canonical(caps)) }` and `PolicyCaps = { capabilities, leash_caps, issuer, subject }` — so leash_caps IS inside caps. Good. But the design should state canonical() covers ALL of PolicyCaps. It does by construction (canonical(caps)). OK so the main finding is: canonicalization is unspecified; mandate sorted-keys JSON with strict typing. MEDIUM.

**Attack surface 5: REPLAY / BINDING** — covered above: subject vs unresolved path. Also: is the subject check done at verify time? Design step 1: "verifies the grant (signature over the canonical caps, and caps.subject == this session's workspace — no replay onto another session)." Yes, at verify time, per action. Good. The workspace-as-subject soundness: needs resolve(). Also: **what is "this session's workspace" compared as — str or Path?** `Path("/a/b") == Path("/a/b")` fine; but if subject is stored as a string in JSON and compared to `Path`, `"/a/b" == Path("/a/b")` is False in Python (str vs Path) → everything fails closed (safe but broken). Implementation detail; the design should pin the comparison (string of the resolved path). Fold into the subject finding.

Another replay angle: **the grant is per-workspace but the SAME workspace string can back multiple sessions with different policy_keys.** The grant is verified with `session.caps_key`. If two sessions share a workspace and caps_key, a grant replays across them — but they're the same trust domain and same subject, so that's within the binding's intent. Fine.

**Cross-tool leash cap:** `leash_caps: {tool: max_looseness}` keyed by tool NAME. Tools are a fixed registry. Fine.

**Now the approve-path capability re-gate:** `reauthorized_or_denied` currently reads `tuple(session.capabilities)`. Design says it will source from verified grant. Good — and this is the TOCTOU re-gate, so it MUST re-verify the grant at approval time (not cache the origination verification). The design says "every governed action verifies" — approval is a governed action (it runs). Should be explicit that the re-gate re-verifies (signature+subject) at approval, not reuse a cached result — actually caching within a session is fine since caps can't change without the key... but the SESSION's caps blob could be swapped (attacker replaces `session.policy_caps` with a different validly-signed grant — e.g., a broader grant minted earlier for the same workspace, an OLD grant). **Grant rollback/replay of an older, broader grant:** if the authority minted a broad grant, later minted a narrower one, and the session's blob can be swapped back to the broad one (both validly signed, same subject) — no nonce/version/expiry means the OLD broad grant still verifies! This is a real replay/rollback issue: **without a version/nonce/expiry, a previously-issued broader grant for the same subject remains valid forever; "revocation" by re-minting a narrower grant does nothing if the old blob is still around (and in a single trust domain with mutable session state, swapping the blob is the same class of mutation as editing caps).** The design doesn't mention this at all. Severity: MEDIUM (within the single-domain model, the attacker who can swap session.policy_caps to another valid blob is close to the in-process adversary, but NOT the same as a re-signer — they don't need the key, just access to an old blob, which might be on disk/in logs). The honest fix: name it — either a monotonic version/not-before with the host pinning the minimum, or explicitly defer with a sentence: "an older validly-signed grant for the same subject remains acceptable; revocation = key rotation." The doc currently doesn't mention revocation at all. I'll fold nonce/expiry/rollback into one MEDIUM finding: honestly deferred is fine, but the doc is SILENT, and the rollback-via-old-grant scenario should be named since it's cheaper than re-signing.

**Now let me also check: does the design's step 2 break the existing core gate?** "sources the capability set from the verified caps, then feeds that to the existing core issue_policy/interpret capability gate." So `issue_policy(..., granted_capabilities(session), ...)`. Fine.

**The `leash` recorded in Decisions:** with caps, the recorded leash should be the EFFECTIVE (capped) leash for audit honesty. `execute_and_verify` re-derives uncapped `_leash_for` — so the Decision could record `act_then_report` while the cap was `propose_first`... but as established, if cap were propose_first the action wouldn't reach execute_and_verify from govern_action. From the APPROVE path it would, and there the recorded leash = uncapped host leash. If host set act_then_report but cap propose_first, approval runs it (correct — approved), and records leash=act_then_report. Is that dishonest? The action ran after approval under a propose_first cap; recording "act_then_report" misrepresents the governing leash. Minor audit-honesty issue, fold into the approve-path finding.

**Pause path:** `if getattr(session, "paused", False): return PAUSED` — happens BEFORE verification. Paused → held, not run. Fail-safe. Fine. But note: pause check happens before grant verification — no authority leak (nothing runs). Fine.

**UNKNOWN_TOOL path:** returns before verification — no authority. Fine.

**Signals emitted before capability gate:** `_emit_signals` publishes to bus even for actions that will be denied — pre-existing, influence-only. Fine.

**One more mediation path:** `propose.py` (Step 1) — not shown. The propose channel surfaces proposals; `approve_proposal` runs them. The design's "What gets built" only modifies `governance.py` (govern_action + reauthorized_or_denied), `session.py`, new `policycaps.py`. If `propose.py` has its OWN capability check or leash resolution (it creates HELD decisions — presumably via govern_action), it likely funnels through govern_action. But `approve_proposal` — does it call `reauthorized_or_denied`? The docstring of `reauthorized_or_denied` says it's the re-gate at the moment of use, so presumably approve_proposal calls it. The design names reauthorized_or_denied explicitly — good. But it does NOT name `approve_proposal`/`propose.py` in "What gets built." If approve_proposal does anything with leash or capabilities beyond reauthorized_or_denied, it's unmediated. The approve-path leash-cap finding covers this.

**`granted_capabilities(session)` when caps present and valid but EMPTY capabilities:** returns () → everything denied. Fail closed. Good.

**`caps_key` provisioning:** "v0 takes the key as host input, exactly as the executor key does today." Note the DEFAULT policy_key is `b"collab-policy-key"` — a hardcoded default! If caps_key follows the same pattern with a hardcoded default, then "the key" is public knowledge → anyone can re-sign → the whole edifice is theater even within its own model. The design says "takes the key as host input" — must NOT default to a hardcoded value. Session's existing `policy_key: bytes = b"collab-policy-key"` default is a bad precedent. **Finding: caps_key must have NO default (required when policy_caps is provided); a hardcoded default key would let any component re-sign, collapsing even the tamper-evidence claim.** MEDIUM/HIGH. The design says "takes the key as host input, exactly as the executor key does today" — the executor key today HAS a default (`b"collab-exec-key"`)! So "exactly as today" implies a default. That's a real trap. I'll flag it: the design must explicitly deviate from the executor-key pattern here — no default caps_key; absent key with caps present fails closed (which the design DOES say — "Absent key when a grant is present ⇒ fail closed" — but if Session gives caps_key a default value, "absent" never happens and the default key is universally known). Actually if the default is applied at construction, then "absent key" can't occur and the default key signs everything → tamper-evidence gone (anyone can mint). So the fail-closed-on-absent-key property is INCOMPATIBLE with a defaulted key. The design must say: caps_key defaults to None, and None + caps present → fail closed. HIGH-ish, but it's a design-clarity issue; the design's own property implies None-default. I'll call it MEDIUM with a sharp scenario, or HIGH... Let me think: the design says "Absent key when a grant is present → fail closed," which logically requires key-can-be-absent. And "v0 takes the key as host input." A reader could implement `caps_key: bytes = b"collab-caps-key"` mirroring policy_key. The finding: make the None-default explicit. MEDIUM.

**Now, severity calibration and ordering.** Let me also double check the stricter() ordering question once more, since the prompt explicitly asks. Ranks: act_then_report=0, propose_first=1, notify_only=2. effective = max(host, cap). Cap propose_first=1: host act_then_report=0 → 1 (held) ✓; host propose_first=1 → 1 ✓; host notify_only=2 → 2 ✓. No off-by-one. The formula is correct. The implementation risk: `_VALID_LEASHES` ordering in governance.py is `(ACT_THEN_REPORT, PROPOSE_FIRST, NOTIFY_ONLY)` — if someone uses `max(host, cap)` on the STRINGS, "propose_first" > "notify_only" > "act_then_report" alphabetically? "act_then_report" < "notify_only" < "propose_first" alphabetically. max("propose_first","notify_only") = "propose_first" — WRONG (notify_only is stricter). So string comparison would be a live bug; the design correctly specifies rank integers. Worth a line in the review: implement via the rank map, never string comparison. LOW/note.

Also `apply_cap(leash, cap)` signature: cap could be None (tool not in leash_caps) → behavior unspecified (the missing-cap finding).

**Also check: does the design cap the leash on the NOTIFY path correctly?** If cap is notify_only and host leash is act_then_report → effective notify_only → NOTIFIED. Good.

**What about `importance`/`risk`?** Influence only, out of scope.

**The `subject` binding and `Session.workspace` being mutable:** `session.workspace` is a plain attribute — an attacker could mutate `session.workspace` to match a grant's subject! Scenario: grant minted for workspace A (broad caps). Attacker (config-flip class) sets `session.workspace = Path("A")` on a session whose real workspace is B... but then the session's tools operate on A, not B — the workspace IS the authority boundary for paths. Changing session.workspace changes where tools act; the grant then correctly applies to A. Is that a bypass? The attacker wanted to act in B with A's caps — but the workspace pre-check confines paths to session.workspace (=A now). So they can't act in B with A's grant. They could act in A with A's grant — which is what the grant authorizes. So mutating workspace doesn't widen past the grant. BUT: mutating `session.workspace` to A lets a session with A's GRANT... wait, the session must also HAVE A's grant blob. If the session has no grant, legacy path. If it has a grant for B and attacker flips workspace to A: subject B ≠ A → fail closed. If attacker has access to A's grant blob (rollback/replay class), sets session.policy_caps = A's grant and session.workspace = A → acts in A with A's authority. That's... exactly what A's grant permits. Not a widening. OK, workspace mutability is fine within the model. The real subject issue remains resolve()/relative paths.

Hmm, one more: **the subject check compares to session.workspace, but the grant should arguably bind to the RESOLVED workspace.** If the authority mints with subject = resolved("/home/u/proj") but Session stores "/home/u/proj/../proj", verify fails (safe). If authority mints with subject = "proj" (relative, because ops passed a relative path), then ANY session with workspace "proj" in ANY cwd verifies → cross-directory replay. So the fix is resolve at mint AND verify, and the design should mandate it. Confirmed MEDIUM.

**Legacy path as a hole (attack surface 6):** The big one is grant-stripping (covered). The secondary: is opt-in hardening acceptable? Yes, honestly — you can't force hardening on existing sessions without breaking them; the doc says "the signed path is opt-in hardening." But the doc should state the obvious: **if you don't attach caps, you get NO new protection, and an attacker who can strip caps gets legacy.** The doc's backward-compat bullet says "behaves exactly as today" without naming the stripping risk. Fold into the stripping finding.

**Let me also check `reauthorized_or_denied` leash handling:** it takes `leash` as a param (the origination leash) and uses it only for the Decision record. It does NOT re-resolve or cap the leash. Per the design, should approval re-check the leash cap? The cap is immutable, origination enforced it... but the approve path's enforcement question stands: what enforces a `notify_only` cap at approval? If origination capped to notify_only → NOTIFIED → no proposal → no approval. But proposals originate from the PROPOSE channel (Step 1), which may not go through govern_action's leash branch at all (it creates its own HELD decisions — `p.decision`). We can't see propose.py. If propose.py calls govern_action, caps apply. The design's "What gets built" doesn't touch propose.py — so either proposals flow through govern_action (fine) or they don't (hole). **Finding: the design doesn't state that the propose channel's origination goes through the capped path, and doesn't enforce the cap at approval; a notify_only cap must mean "never runs," which requires enforcement at the moment of use (approve), not only at origination.** This is my top finding. HIGH.

Let me also sanity-check the claim "config can TIGHTEN but never WIDEN past the grant" against `set_leash` rejecting invalid values: `set_leash` rejects invalid leashes (returns False). With caps, set_leash can still WRITE act_then_report into overrides; the cap is applied at govern time. Fine — the override is inert. But the VIEW displays `leashes` from overrides — so the view would SHOW act_then_report while the effective is propose_first. Display honesty again (same finding as capabilities display). The view should show effective (capped) values. LOW/MEDIUM. I'll merge display issues into one LOW finding: the view renders mutable config (capabilities + leashes), not the verified/capped effective authority — the host looking at the control surface sees authority the grant doesn't confer (or a leash the cap forbids), undermining the "put a hand on it" trust. Fix: view renders granted_capabilities() and effective leashes.

**Structure of findings:**

1. **CRITICAL/HIGH — Grant-stripping fails open (legacy path as bypass).** Location: design "Backward compatible" property + step 1 "absent-when-required" (undefined) + session.py (policy_caps optional). Scenario: the same config-flip threat the design names (injected mutation in another component) sets `session.policy_caps = None`; `granted_capabilities` falls back to `session.capabilities` (mutable) and no leash caps apply — full widening, exactly what ③ claims to close, achieved by DELETING rather than editing the grant. The fail-closed list covers tamper/absent-key/wrong-subject/malformed but not absent-grant. Fix: stickiness — constructor records that caps were provisioned (e.g., `_policy_caps_required = policy_caps is not None`), and `granted_capabilities`/`leash_cap` fail closed when required-but-absent; define "required" in the doc; add proof #7 "strip the grant → denied." Severity: HIGH (it's the cleanest bypass of the whole scheme within the design's OWN stated threat model — a non-crypto mutation in a different component). Could argue CRITICAL. The threat model says the realistic threat is "a bug or an injected config flip in a different component that doesn't go through mint" — deleting an attribute is exactly that. I'll say HIGH (reserving CRITICAL for something like a default key or a verify-pass bug; actually let me think — is this CRITICAL? It's a complete bypass of the hardening via the documented legacy path, requiring only the already-in-model capability of mutating session config. The design half-addresses it with "absent-when-required" but never defines required. I'll call it HIGH, and note it's the finding that most threatens the core claim. Hmm, the prompt says CRITICAL|HIGH|MEDIUM|LOW. I think grant-stripping is HIGH bordering CRITICAL; since the design gestures at "absent-when-required," it's a spec gap rather than a blind spot — HIGH.)

2. **HIGH — Leash cap not mediated on the approve/re-gate path (notify_only cap can be bypassed; cap enforced only at origination).** Location: design step 3 + governance.py `execute_and_verify` (re-derives `_leash_for` uncapped) + `reauthorized_or_denied` (no leash re-check) + "What gets built" (propose.py untouched). Scenario: grant caps run_command at notify_only ("never auto-run, never run — only surface"). A proposal for run_command is surfaced (propose channel) and the host (or a compromised control surface calling approve) approves it; `approve_proposal` → `reauthorized_or_denied` (capability OK — shell.exec IS granted; the cap is a LEASH constraint) → `execute_and_verify` runs it. The notify_only cap, enforced only in govern_action's branch, never fires on the approve path. Also: `execute_and_verify` records the uncapped leash. Fix: enforce the cap at the moment of use — `reauthorized_or_denied` (or approve_proposal) must compute `effective = apply_cap(_resolve_leash(...), leash_cap(session, tool))` and refuse (DENIED) or hold when the effective leash forbids running; and `execute_and_verify` must receive/record the effective capped leash, not re-derive from mutable config. Also state that propose-channel origination passes through the capped govern_action. Severity HIGH.

Wait — I should double-check the semantics: is notify_only meant as "never run"? From tools.py: `NOTIFIED = "notified"  # notify_only: surfaced, not run`. And govern_action: `if leash == NOTIFY_ONLY: return Decision(..., NOTIFIED, ...)` — not run. So yes, notify_only = never run. A cap of notify_only = "this tool must never run under this grant." If approval can run it, the cap is meaningless on that path. But hold on — can a proposal even exist for a notify_only-capped tool? The propose channel (Step 1, proactivity dial) surfaces proposals; approve_proposal runs them. If proposals are created via govern_action, a notify_only effective leash yields NOTIFIED, not a proposal. But the design doesn't say propose.py goes through govern_action, and even if it does TODAY, the cap's enforcement point should be at the moment of use for the same TOCTOU reason the design cites for capabilities. Actually the cleaner scenario that works EVEN IF proposals go through govern_action: cap is propose_first; action HELD at origination; host's per-task leash kwarg or override changes... no. Let me construct the cleanest: cap = notify_only is the clean case for "never runs." For the approve path to be reached, a HELD decision must exist. If origination enforces the cap, a notify_only-capped tool never produces HELD. So the bypass requires proposals to originate OUTSIDE govern_action's capped branch — which the design doesn't rule out (propose.py not in "What gets built," not shown). Alternatively, the simpler framing: the design claims "every governed action" verifies + caps, but the approve path is a governed action (it runs a tool) and the design only specifies CAPABILITY re-gating there, not leash-cap enforcement. Even if today's propose.py funnels through govern_action, the invariant "notify_only cap ⇒ never runs" rests on an enforcement point at origination only, while the design's own TOCTOU doctrine (F1) says authority must be re-derived at the moment of use. The leash cap is authority. So: enforce at use. HIGH, with the honest caveat that exploitability depends on propose.py's wiring which the design doesn't show — itself a documentation gap.

Hmm, actually, let me reconsider — is there a scenario where the approve path runs something the cap forbids EVEN with origination enforcement? The cap is signed/immutable, so no TOCTOU on the cap. The host leash can change (set_leash to act_then_report after origination) — but approval runs the action anyway (that's what approval means under propose_first). Under a notify_only cap, no HELD exists. So the ONLY hole is proposals originating outside the capped path. Given we can't see propose.py, I'll frame it as: (a) the design must state all origination flows through the capped govern_action; (b) defense-in-depth + consistency with F1: re-apply the cap at the moment of use in reauthorized_or_denied/approve; (c) execute_and_verify must not re-derive the uncapped leash. HIGH feels right given notify_only's "never run" semantics; could be MEDIUM if proposals provably flow through govern_action. I'll say HIGH with that note.

3. **MEDIUM — caps_key default / provisioning ambiguity can nullify even tamper-evidence.** Location: design "The decisions that stay the host's" + session.py precedent (`policy_key: bytes = b"collab-policy-key"`). Scenario: implementer mirrors the existing pattern, `caps_key: bytes = b"collab-caps-key"`; now every process knows the key, any component can mint/re-sign, and "absent key → fail closed" can never trigger (the default is always present). The design's own fail-closed property requires key-absence to be representable. Fix: `caps_key=None` default; None + policy_caps present ⇒ fail closed; doc explicitly says "unlike policy_key/executor_key, caps_key has NO default." MEDIUM (design ambiguity with catastrophic failure mode if misread).

4. **MEDIUM — Subject binding uses unresolved workspace path; canonical form unspecified.** Location: design step 1 ("caps.subject == this session's workspace") + session.py (`self.workspace = Path(workspace)`, no resolve). Scenario: authority mints a grant with subject "proj" (relative) or a symlinked path; the same grant verifies on any session whose workspace STRING matches in any cwd — replay across directories; conversely resolved-vs-unresolved mismatches fail closed (availability, not security). Also str-vs-Path comparison must be pinned. Fix: subject = `str(Path(workspace).resolve())` at BOTH mint and verify; document; consider os.path.normcase on case-insensitive filesystems (or note the limitation). MEDIUM.

5. **MEDIUM — Canonicalization unspecified ("canonical(caps)").** Location: design, PolicyCaps definition. Scenario: implemented as repr()/str(dict)/naive join → ambiguity (tuple vs list, int vs str, separator-in-value, key ordering) can make two different PolicyCaps produce the same signed bytes (or the same caps produce different bytes → fail closed). Exploit: get authority to sign a benign grant, present a mutated caps blob whose canonical form collides — e.g., unknown-field dropping or type confusion. Fix: pin `json.dumps(schema_checked_caps, sort_keys=True, separators=(",",":"))`, strict schema (capabilities: sorted list[str]; leash_caps: dict[str, enum]; issuer/subject: non-empty str; no unknown fields), and state that canonical() covers ALL of PolicyCaps. MEDIUM.

6. **MEDIUM — No revocation/rollback story: an older, broader validly-signed grant for the same subject remains valid forever.** Location: design (silent on expiry/version/revocation). Scenario: authority mints a broad grant (shell.exec, act_then_report caps) for workspace W; later tightens to a narrow grant; the broad blob persists (logs, disk, backups, an old session object). Anyone who can swap `session.policy_caps` (the same config-mutation class as tamper, but NO key needed — the blob is validly signed) rolls back to the broad grant; verify passes (signature valid, subject matches). This is strictly cheaper than re-signing and is NOT covered by the honest-scope section (which only admits the re-signer). Fix: name it — either add a monotonic `version`/`not_before` with the host pinning a minimum accepted version, or explicitly document "revocation = caps_key rotation; old grants for a subject remain valid until then." MEDIUM (honesty gap + real rollback vector).

7. **MEDIUM/LOW — Missing per-tool leash_cap default unspecified.** Location: design step 3 / `leash_cap(session, tool)`. Scenario: grant lists capabilities including shell.exec but its leash_caps omits run_command; if leash_cap returns None and apply_cap treats None as "no cap," run_command's leash is governed by mutable host config — the grant's silence confers looseness, contradicting "the grant is the complete statement of authority." Fix: absent per-tool cap ⇒ strictest (notify_only), or at minimum stricter(tool_default, ...); document. I'd say MEDIUM... it's arguably a design choice; but "silence confers looseness" in a fail-closed system is wrong. MEDIUM.

8. **LOW — The view displays mutable config as authority.** Location: view.py snapshot() (`capabilities` from session.capabilities, `leashes` from leash_overrides). Scenario: after the proof-#1 attack (attacker sets session.capabilities to include shell.exec), the gate denies but the VIEW shows shell.exec granted — the host is told authority exists that the grant doesn't confer; conversely a loosened override displays though the cap forbids it. Fix: view renders granted_capabilities(session) and apply_cap-capped effective leashes (and marks legacy sessions). LOW.

9. **LOW — Docstring/claim drift:** governance.py govern_action docstring says "A host leash may tighten OR loosen relative to the tool default (host authority over their own leash)" — with caps present this is false (loosening bounded by cap); the design's "can only ever operate within the signed grant" is true only after findings 1–2 are fixed. Also the proof list omits: strip-grant, absent-key, malformed, re-gate-against-grant (mutate caps between hold and approve), and approve-path cap enforcement. Fix: update docstrings; extend proof list. LOW.

10. Maybe: **verify-before-pause ordering / signals on denied** — not issues. Skip.

11. **Rank implementation note:** implement stricter via the integer rank map, never string max() (alphabetical order ≠ strictness order: "propose_first" > "notify_only" lexicographically). Fold into a LOW note or into finding 7/9. I'll make it a brief LOW or fold it. I'll fold into #7's fix or make a small LOW. Let me fold it into the stricter discussion as part of finding on ordering — actually the prompt explicitly asks about off-by-one in rank ordering; I should explicitly CLEAR it: the stated ordering and max() are correct (act_then_report=0 cannot pass a propose_first=1 cap). I'll say so in the steelman/verdict or as a "checked and sound" note. Maybe include a LOW: "ordering is correct as specified; the only risk is an implementer comparing strings — pin the rank map in code." LOW.

Also should I check: **does govern_action verify BEFORE emitting signals/pausing?** Order in the design: verify first. In code, pause-check happens before the try block. If verification is added inside the try (with issue_policy), then a paused session returns PAUSED without verifying — fine (nothing runs). Signals emitted for soon-to-be-denied actions — pre-existing. Not findings.

**One more mediation check:** `govern_action`'s `leash` kwarg is keyword-only host authority. With caps, the kwarg must ALSO be capped. Design: "resolves the leash as today, then caps it" — "as today" = _resolve_leash which handles the kwarg. So capped. Good — but the docstring says the host leash "may tighten OR loosen" — covered in finding 9.

**Is there any path where an action runs before verification?** govern_action: unknown tool → return (no run). paused → return. try{issue_policy, signals, interpret} → gate → workspace pre-check → leash branches → execute. Verification of the GRANT must happen before issue_policy uses the caps. If granted_capabilities() is called to build the policy, verification is inherently before the gate. OK. The only pre-verification runs: none. Good.

**What about `session.caps_key` being used per action — performance, fine.**

**Empty subject string:** if subject == "" and session workspace == ""? Session requires a workspace; Path("") → Path("."). A grant with subject "" must be rejected as malformed. Fold into #5's strict schema (non-empty subject) or #4. I'll mention in #4.

Now, the STEELMAN: The design is unusually honest about its trust domain; the core mechanism (verify-then-source, cap-by-max) is the right shape; the seam already fails closed on errors and the two named call sites are the only capability reads in the governance path; the rank math is correct; subject binding exists at verify time per action.

VERDICT: MINOR_ISSUES or SERIOUS_FLAWS? The core design is sound and honestly scoped, but findings 1 (grant-stripping fail-open) and 2 (approve-path cap mediation) are real holes in the two properties the design exists to guarantee (fail-closed, no-widen). They're fixable in spec without redesign. I'd say... the prompt defines verdicts as SOUND / MINOR_ISSUES / SERIOUS_FLAWS + one sentence. Two HIGH findings that undermine the headline properties → SERIOUS_FLAWS feels right? Or "MINOR_ISSUES" because they're spec gaps easily closed? The grant-stripping hole means the hardening can be silently switched off by the exact threat it names — that's not minor. The approve-path gap means a "never run" cap may not mean never run. I'll go SERIOUS_FLAWS — but let me weigh: the design DOES say "absent-when-required ⇒ fail closed," so a charitable reading covers stripping; but "required" is undefined and the backward-compat bullet flatly says "no PolicyCaps ⇒ legacy," and the proof list has no stripping case. A design review should flag that the two bullets contradict unless "required" is defined. And the approve path is genuinely unspecified. Two HIGHs + several MEDIUMs → SERIOUS_FLAWS with the sentence noting they're spec-completeness flaws, not architectural ones, and the architecture is right.

Hmm, let me reconsider — is that too harsh? The calibration says "If sound with only minor notes, say so plainly." This design is NOT sound-with-minor-notes: it has a fail-open legacy toggle reachable by its own named threat, an unmediated authority path (approve) for one of its two axes, an unspecified canonicalization, an unspecified subject canonical form, a key-default trap, and a silent revocation gap. But the architecture and honesty framing are good. SERIOUS_FLAWS = "the properties as specified don't yet hold; fixable without redesign." I'll go with that, one sentence.

Let me now also double-check finding 1's fix feasibility: constructor stickiness. `Session.__init__` gains `policy_caps=None, caps_key=None`. If policy_caps provided → set `self._policy_caps_required = True`. `granted_capabilities(session)`: if `getattr(session, "_policy_caps_required", False)` and `session.policy_caps is None` → return () (fail closed). But `_policy_caps_required` is itself mutable... in a single trust domain, everything is; the point is raising the bar so that the SPECIFIC named threat (config flip in another component, control-surface misuse) can't accidentally or easily disable it, and so the property is at least well-defined. Also the honest note: against an arbitrary-attribute-writer, no Python attribute is safe — but that attacker is already the scoped-out in-process adversary (they could just call execute_tool directly, bypassing governance entirely!). Actually THAT's an important calibration point: a fully in-process adversary doesn't need to strip caps — they can import execute_tool and run it, skipping govern_action. So the stripping threat only matters for the "different component / config flip / injected value" class, where the flip is blind or partial. For a blind config-flipper, `session.policy_caps = None` is plausible. So the finding stands at HIGH but I should note the calibration: governance is opt-in mediation; a component that can call tools directly is already outside it. The design's seam is "every tool action mediated" by convention. Fine.

Also for finding 2, let me make sure about `approve_proposal` — it's imported in view.py from collaborator.propose. Not shown. The re-gate docstring says approval re-checks. So approve_proposal presumably: reauthorized_or_denied(session, tool, ...) → if None → execute_and_verify(...). The leash passed to execute_and_verify is re-derived uncapped. And no cap check. So the finding is: design must add cap enforcement to the re-gate. Also note: `reauthorized_or_denied` re-issues policy with `tuple(session.capabilities)` TODAY; design says it will use verified caps — good, but the proof list should include the TOCTOU case (mutate session.capabilities between hold and approve → still denied) to prove the re-gate reads the grant.

Let me also double check finding 6's scenario validity: does anything prevent blob swap? `session.policy_caps` is a plain attribute. Swapping requires the same access as editing caps — the design's named threat. Editing caps without re-signing → detected. Swapping the whole SignedPolicyCaps for ANOTHER validly-signed one (same subject) → NOT detected (signature valid). Where would an attacker get another valid blob? Any previously-issued grant for that workspace (broader, older). In v0 with no versioning, the authority has no way to say "grant v2 supersedes v1." Real. MEDIUM. And the honest-scope section's admission ("a fully-adversarial component that holds the key could re-sign") does NOT cover this — no key needed. So it's an honesty gap too. Good finding.

Also — should the subject bind to workspace ONLY, or also to something like the policy_key/executor? If the same workspace is used with a different executor... out of scope, fine.

Let me also verify the claim in finding 3 about "absent key": design property says "Absent key when a grant is present → same [fail closed]." So the design DOES require key-absence to be possible. Session must default caps_key to None. The risk is only that "exactly as the executor key does today" suggests a default. So finding 3 is really "make the None-default explicit; the executor-key analogy is dangerous." MEDIUM→LOW? I'll keep MEDIUM because if misread it collapses everything, but acknowledge the design's property list implies the right thing.

Let me also reconsider: is there a finding about `granted_capabilities` being called per action and the signature verification happening per action — HMAC is cheap, fine.

**Format:** The user wants for EACH finding: ID / TITLE / SEVERITY / LOCATION / CONCRETE SCENARIO / WHY IT MATTERS / SUGGESTED FIX. Then STEELMAN (2-3 sentences) and VERDICT. I should also explicitly answer the calibration questions where they're NOT findings (e.g., rank ordering is correct — say so; single-domain scoping is acceptable — say so; don't demand asymmetric crypto). I can weave those into a short "checked and cleared" section or into steelman. The prompt says attack hardest in order 1-6; I'll order findings by severity but reference the attack order. Maybe organize findings roughly by the attack-surface order with severities marked. Let me order: F1 stripping (HIGH), F2 approve-path cap (HIGH), F3 caps_key default (MEDIUM), F4 subject canonicalization (MEDIUM), F5 canonical(caps) unspecified (MEDIUM), F6 rollback/no-revocation (MEDIUM), F7 missing per-tool cap default (MEDIUM), F8 view displays mutable authority (LOW), F9 claim/docstring drift + proof gaps (LOW), F10 rank-string-comparison trap (LOW, with explicit "the math as specified is correct").

That's 10 findings — comprehensive but each concrete. Maybe merge F10 into F7 or keep separate small. I'll keep 10 but tight. Actually, let me merge F9's proof-gap part and docstring part — fine as one. And I might add an explicit "CLEARED" note section briefly: rank math correct; both capability call sites named; fail-closed on tamper/absent-key/wrong-subject/malformed is correctly specified; single-domain scoping itself is acceptable and consistent with ADR 0002; no asymmetric crypto demanded. That addresses the calibration instruction directly.

Length: this needs to be thorough — it's a security review with a mandated format. Headers per finding with the fields. I'll use markdown headers/bold fields. Keep each finding tight but concrete. Probably ~1200-1800 words. Let me write it.

Let me draft each finding precisely.

---

**F1 — Grant-stripping fails open: deleting `session.policy_caps` reverts to mutable-config authority**
SEVERITY: HIGH
LOCATION: Design §"The properties it must hold" (Backward compatible) + step 1 ("absent-when-required" — never defined) + session.py (policy_caps to be optional).
SCENARIO: The design's own named threat — "a bug or an injected config flip in a different component that doesn't
