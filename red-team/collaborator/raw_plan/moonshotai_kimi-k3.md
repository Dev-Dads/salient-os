# Collaborator-plan red-team: moonshotai/kimi-k3

_finish=error seconds=435.8 usage={'prompt_tokens': 14879, 'completion_tokens': 5070, 'total_tokens': 19949, 'cost': 0, 'is_byok': False, 'prompt_tokens_details': {'cached_tokens': 0, 'cache_write_tokens': 0, 'audio_tokens': 0, 'video_tokens': 0}, 'cost_details': {'upstream_inference_cost': 0.11465265, 'upstream_inference_prompt_cost': 0.04240515, 'upstream_inference_completions_cost': 0.0722475}, 'completion_tokens_details': {'reasoning_tokens': 5070, 'image_tokens': 0, 'audio_tokens': 0}}_

Let me carefully work through this adversarial review. I need to attack the plan hardest, in the order given:

1. P-01 INTEGRITY — salience buying authority, governance seam bypass, leash/trust dial separation
2. SEAM FEASIBILITY & COMPLETENESS — can "EVERY action mediated before it happens" be built given the real flow
3. THE HONESTY CLAIM — "hands that can't lie about what they did"
4. FAIL-SAFE POSTURE — default safe when confused/adversarial/absent
5. THE STAGE-4 TIE-IN — adaptation-eligible verified actions from a chat/tool loop
6. SCOPE HONESTY — is Step 0 coherent, hiding dependencies
7. MISSING PIECES — concurrency, multi-step plans, audit tamper-resistance, verification evidence source, trust boundary of conversational surface, off switch, reversibility

Let me dig into the actual API facts to find concrete failures.

**Key API facts:**

- Flow: `issue_policy(...)` → emit `SalienceSignal`(s) (bounded ref-tokens only; a signal CANNOT carry a prompt/body/args by construction) → `interpret(policy, signals, key)` → `Directive` → `govern(...)`/`decide(directive, verdict)` → `GovernedOutcome` → `consume(outcome, now_days)` → `(AdaptationDecision, MemoryRetention)`.

- BINDING KEY: `decide()` binds only when `directive.subject == verdict.envelope_id`. Each action must use ONE id as both salience subject and verifier envelope_id, or outcome comes back unbound ("act on nothing").

- Core is stdlib-only, no-async, AST-enforced. Collaborator must be a sibling package.

- Current rig parses ONLY structured tool_calls; plain-text tool calls silently dropped. Plan's "tool-reading we control" fixes this.

- Two learning channels dormant unless host drives `allow_adaptation=True`, verified path.

**Now let me hunt for concrete findings.**

### P-01 INTEGRITY analysis

The plan says: "Importance (salience) moves the *scrutiny* and *compute* dials; **policy** — your trust setting and the leash — is the only thing that moves the *authority* dial."

The leash: act-then-report / propose-first / notify-only. This is authority (whether the action runs before or after human approval). The plan says the leash is "set per task" and is policy. Good — that's the right separation in prose.

But wait — where does the leash come from mechanically? The plan says "The **leash** is the steering wheel, set per task." Set by whom? If the leash is set per task by... the model's interpretation of the task? Or by the human? The plan says "you give a plain instruction; the Collaborator shows you the governed task it will run (what it understood, its reach, its trust and leash); on your confirm it executes."

Hmm — so at Step 0, the human confirms the task including its leash. But then "a **low-stakes** action on `act-then-report` runs and is reported" — so some tasks run on act-then-report without per-action human confirm. Who decided that task was low-stakes and thus act-then-report? If the model's salience assessment (e.g., low RISK signal) can move a task from propose-first to act-then-report, that's salience buying authority — a direct P-01 violation.

The plan's decision #1: "The default leash. When you give a quick instruction, should the default be *propose-first* (safe, asks a lot) or *act-then-report* for clearly reversible work (fast, trusts more)?" — "for clearly reversible work" — who judges "clearly reversible"? If the classifier of reversibility is the model, then model output (influence) is setting the leash (authority). The plan doesn't say the leash assignment must come from policy/host config independent of model output. Actually the honest scope says "The trust/leash lives in host config to start" — so leash is host config. But "set per task" and "adjustable mid-flight" (Step 1) — adjustable by whom? If the model can propose its own leash and the human just confirms, that's okay-ish for Step 0 since human confirms each task. But the danger: salience signals (RISK, ATTENTION) feed interpret(); the directive carries allowed_capabilities from policy only. The leash is NOT part of the Directive at all — the Directive has compute_budget, verification_depth, retention_class, routing_hint, adaptation_eligibility, allowed_capabilities, reconfigure. There is no "leash" field. So the leash is a host-side concept. That's fine — but the plan must guarantee the leash is derived from policy (host config), never from signals. The plan says this in prose ("policy — your trust setting and the leash — is the only thing that moves the authority dial") but the mechanism "set per task" is unspecified: per-task leash could be set by the model's own assessment unless the plan pins that leash assignment is a host/policy function of the action type, not of salience.

Concrete failure: model wants to run `rm -rf ~/Documents`. It emits a low RISK signal (or no RISK signal) about the action. If leash assignment consults the model's risk self-assessment, the task gets act-then-report and runs before the human sees it. The plan needs: leash = f(policy, action class) where action class is determined by the host's own inspection of the tool call (tool name + args pattern), never by model-emitted signals.

Actually, let me re-read: "The leash is the steering wheel, set per task" and decision #1 asks about "the default leash" — implying there's a default plus per-task overrides. The plan doesn't state who overrides. This is a genuine HIGH finding: the plan must state explicitly that the per-task leash is assigned by host policy (from the host's own classification of the proposed action), and that model output can only ever *tighten*... no wait, model output must not tighten either — actually model proposing a tighter leash is safe. The dangerous direction is model influence loosening the leash. The plan should say: salience/model output may never loosen a leash; only the human (or host policy) sets it, and the model may at most propose, with the stricter of policy-default and proposal applying... hmm, actually simplest: leash assignment is policy-only; the model may request a stricter leash but never a looser one than policy grants. That mirrors the verifier's upward-only escalation. Good fix.

Second P-01 angle: the "off switch that returns it to a plain, ungoverned tool-runner." Wait — the plan says Step 0 includes "an off switch that returns it to a plain, ungoverned tool-runner so you can see the governance was load-bearing." That's a demo feature. But an off switch that returns to UNGOVERNED tool-running is itself a governance bypass path. If the off switch exists in the shipped artifact, anything that can flip it (including the model, if the switch is reachable from the tool surface, e.g., a config file the model can write with its write-file tool!) bypasses the seam entirely. Concrete failure: the Collaborator has a `write_file` tool; the off switch is a host config flag; the model writes to the config file (or a --ungoverned flag file), and next action runs ungoverned. The plan must state the off switch is outside the governed tool surface's reach (e.g., requires human shell access, not reachable via any tool), or better, that the demo off switch is a build-time/CLI flag not present in the running config that tools can touch. This is a real CRITICAL/HIGH finding — the demo off switch is a seam bypass unless fenced. Actually, let me think about severity. It's Step 0, personal system, the off switch is for a demo. But the plan explicitly lists it as a Step 0 proof component. The failure mode is concrete: model with file-write tool flips the switch. I'd call it HIGH (it's a demo affordance, but it directly defeats the core invariant if reachable from tools). The fix is one sentence: the off switch is operator-only, outside the tool surface, and its state is re-read from a source the tools cannot write.

Third P-01 angle: "It comes to you — it notices something worth doing and brings you a proposal" — propose channel is Step 1, "deliberately the most governed." Fine.

Fourth: the trust dial. "the trust dial and the leashes as controls you can put your hand on" (Step 2). Trust lives in host config to start; "the principled home (a signed policy the judgment system carries) is a deliberate follow-up." This is honestly disclosed. But there's a subtle issue: `issue_policy(...)` and `verify_policy(policy, policy_key)` — the policy must be signed/verified with a key. If trust/leash live in host config unsigned, then `interpret()` with an unsigned policy returns `_hard_deny` — EMPTY capabilities. Wait, that's interesting: if the host config trust isn't a signed policy, then per the real API, `interpret(policy, signals, key)` hard-denies unless `verify_policy` passes. So the plan's "trust/leash lives in host config to start" implies either (a) the host signs policies at startup with a key it holds (fine — the provenance is "host config," signed at issue time), or (b) policies are unsigned and everything hard-denies (nothing works). The plan's caveat is honest that provenance hardens later. But there's a real question: where does the policy-signing key live, and can the model's tools reach it? If the key is in host config and the toolset includes read-file/run-command with reach over the host config directory, the model can read the key and... well, it can't call interpret() itself unless it can execute code — with a run-command tool it CAN execute code. Oh, this is a big one.

**The run-command tool is a seam bypass.** The plan's toolset: "Read a file, write a file, run a command, search." If "run a command" is a shell, then the governed action "run a command" passes the seam once, but the command itself can be ANYTHING — including a command that performs actions that were never individually governed. E.g., governing "run `make deploy`" is one mediated action, but `make deploy` does a hundred sub-actions none of which pass the seam. More sharply: the model proposes tool call A (innocuous, e.g., `cat file`) which passes governance, and separately the model could propose `bash -c '...'` containing arbitrary nested actions. The claim "every action it takes on the system is governed" is false under a shell tool unless the plan scopes "action" honestly: the governed unit is the tool call, and a shell command is a single opaque unit whose internal effects are ungoverned. That's the classic confused-deputy / confused-tool problem. The plan should either (a) state honestly that "run a command" is governed as an opaque unit and its leash should default to propose-first always (since its reach is unbounded), or (b) constrain run-command to an allowlisted set of binaries. The plan's decision #2 ("The toolset's edge... files and local commands only, or network and installs too?") shows awareness of reach, but doesn't acknowledge that "run a command" makes the per-action mediation claim only as strong as the command's opacity. This is a HIGH finding under seam completeness: the mediation granularity claim is oversold for shell commands. Also nested/chained tools: `bash -c 'curl ... | sh'` — network + install inside a "local command."

Also: the model editing its own governance inputs. With write-file and run-command tools, the model can edit: host config (trust/leash), the audit trail (tamper), the salience signal emitter code, the policy key... The plan must state the host's own config/keys/audit log are outside the toolset's reach (a filesystem allowlist/jail), or the whole governance story collapses. The plan never mentions a workspace boundary or jail. Decision #2 asks about the toolset's edge in terms of files/network/installs but never says "the tools cannot touch the Collaborator's own config, keys, or audit trail." That's a CRITICAL finding I think — it's the concrete path where governance inputs are model-editable. Actually let me calibrate: v0 personal system, but P-01 integrity and safe default are demanded. The plan's own honesty section says trust/leash in host config — and the toolset includes write file + run command. Unless the host config is outside tool reach, the model can grant itself authority = salience... no wait, not salience — the MODEL (an influence source) seizes authority directly by rewriting policy. That's worse than salience buying authority; it's the agent rewriting the policy. The plan must add: the governed toolset operates within a declared workspace root; host config, policy keys, and the audit store are outside that root and unreachable by tools. CRITICAL.

### SEAM FEASIBILITY & COMPLETENESS

Real flow per action: issue_policy → signals → interpret → govern/decide → consume. Binding: directive.subject == verdict.envelope_id.

The plan says "Every proposed action passes through the judgment system before it happens: a turn opens, the action and its importance are interpreted, and the result is a directive."

Feasibility gaps:

(a) **Where do the signals come from?** Signals carry bounded ref-tokens only, cannot carry prompt/body/args. So the salience signals about an action must be emitted by... the host, based on what? The model can't emit them directly (or can it? The model's output is text; the host parses tool intents; the host could also parse salience assertions from the model — but then the model is asserting its own RISK/ATTENTION, which is influence, fine, but the host must treat model-asserted low-risk as no-information... actually the interpreter treats absent RISK as 1.0 (maximal) — good fail-closed. But a model asserting RISK=0.0 with confidence 1.0 LOWERS verification toward the policy floor. That's allowed (influence, not authority) but note: the model can minimize its own scrutiny by asserting low risk. That's P-01-compliant (scrutiny is influence) but worth noting the plan never says who emits signals. If the host emits signals from its own inspection, fine. The plan is silent. Is this a genuine flaw or a spec detail? The plan says "the action and its importance are interpreted" — importance must come from somewhere. For a plan-level doc, I'd flag it as MEDIUM: the plan should state the signal provenance rule — model-asserted signals are influence-only and the host's own risk classification (from tool+args inspection) is what can raise scrutiny; model-asserted low-risk can lower scrutiny only within policy floor (which the core enforces). Actually the core already handles the floors. The real gap: the plan doesn't say signals derive from host inspection of the parsed tool call (tool name, args hash, ref-token), which is what makes subject==envelope_id binding meaningful. Hmm.

(b) **The binding key discipline.** Each action needs ONE id as both salience subject and verifier envelope_id. The plan never mentions this. It's arguably a spec detail, but it's load-bearing and easy to get wrong: if the host issues one policy per "task" (subject = task id) but the verifier envelopes each tool call (envelope_id = action id), nothing binds and every outcome is unbound → "act on nothing" → fail-closed deny. So the naive reading of the plan ("a turn opens, the action and its importance are interpreted") with per-task policy + per-action verification fails closed (safe but non-functional), and the pressure to make it work creates the temptation to bind loosely (e.g., wildcard subjects). The plan should state: the governed unit is the individual tool action; policy subject, signal subjects, and verifier envelope_id are all the per-action id. MEDIUM-HIGH feasibility finding. Actually since fail-closed is the default, getting it wrong is safe-but-broken; the risk is someone "fixing" it by weakening binding. I'd rate MEDIUM with a note.

(c) **Multiple tool calls in one turn / mid-sentence / streamed partial calls.** The plan's "tool-reading we control" claims "every real tool intent is caught." Streaming: if the model streams a partial tool call, the parser must buffer until complete — a partial call must never be executed. The plan doesn't address partial/streamed intents. Also multiple tool calls in one turn: each must be separately governed (separate subject/envelope). And tool calls emitted mid-sentence inside prose: the plan claims to catch plain-text tool calls — parsing tool calls out of natural language is genuinely hard (the model writes "run: rm -rf /" inside a code block as an EXAMPLE — is that an intent or a mention?). The plan's claim "nothing the model merely narrates is ever mistaken for something that happened" cuts both ways: the parser must also not mistake a *mention* of a tool call (in a code block, in a quote, in a "here's what I would do") for an *intent*. False positives → governing/executing things the model didn't intend; false negatives → dropped intents. The plan oversells "One honest path in: every real tool intent is caught." Distinguishing intent from narration in free text is an unsolved-in-general problem; the honest design is: only execute intents expressed in a defined structured format; treat plain-text tool-looking content as a *parseable intent only if it matches a strict schema*, else surface it to the human as "the model seems to want to run X but didn't say it in a way I can govern — confirm?" Actually the box gap fix should be: detect plain-text tool intent, and rather than auto-executing, either (a) require the model to re-emit in structured form, or (b) treat detection as propose-first regardless. The plan says the plain-text call "actually gets its action run (the box gap, closed)" — auto-running parsed-from-prose intents is dangerous unless the parse is strict and the leash applies. Hmm, but the leash does apply to every action. Still, the intent/mention discrimination problem is real: model writes a tutorial containing "rm -rf /tmp/x" as an example; the over-eager parser runs it. The plan must specify: plain-text intents are accepted only in a single, strict, unambiguous format (e.g., a fenced block with a specific tag), everything else is narration. HIGH finding — this is the seam completeness/honesty intersection and the plan's Step 0 proof explicitly demos auto-running a plain-text tool call.

(d) **Retries/errors.** If a governed tool call fails (tool error), and the loop retries, each retry is a new action needing fresh governance? Or does the cleared outcome cover retries? The plan doesn't say. Spec detail mostly, but there's a subtle issue: governance happens BEFORE the action runs ("mediated before it happens"), but the real flow includes verification (verdict) which is about what ACTUALLY happened — verification is inherently post-hoc (receipt + world evidence). So the actual sequence per action must be: pre-act authorization (policy + interpret → directive with allowed_capabilities; host checks capability grant + leash) → act → post-act verification (govern/decide → cleared/adaptation) → consume. The plan's description "Every proposed action passes through the judgment system before it happens: a turn opens, the action and its importance are interpreted, and the result is a directive" — captures the pre-act half. But `govern()` needs envelope + receipt + world_evidence — i.e., the action must have RUN to produce a receipt. So "every action mediated before it happens" is only half the seam; the plan never describes the post-act verification half for tool actions, nor what "cleared" means for an action that already ran (you can't un-run it). This connects to finding (e):

(e) **Where does verification evidence come from for tool actions?** (Also Missing Pieces #7.) `govern(verifier, directive, envelope, receipt, world_evidence)`. For a file write: receipt = tool's own record; world evidence = re-reading the file? For run-command: receipt = exit code + output; world evidence = ??? Independent sources for HIGH stakes require two independent channels. For most tool actions, there IS no second independent source — the tool's own report and the filesystem are not fully independent if the tool wrote the file (actually re-statting the file IS independent of the tool's report — the tool
