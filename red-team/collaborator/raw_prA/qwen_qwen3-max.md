# PR-A host-emit code panel: qwen/qwen3-max

_finish=stop seconds=29.4 cost=$0.0897 usage={'prompt_tokens': 50253, 'completion_tokens': 1448, 'total_tokens': 51701, 'cost': 0.08968908, 'is_byok': False, 'prompt_tokens_details': {'cached_tokens': 0, 'cache_write_tokens': 0, 'audio_tokens': 0, 'video_tokens': 0}, 'cost_details': {'upstream_inference_cost': 0.08968908, 'upstream_inference_prompt_cost': 0.07839468, 'upstream_inference_completions_cost': 0.0112944}, 'completion_tokens_details': {'reasoning_tokens': 0, 'image_tokens': 0, 'audio_tokens': 0}}_

## Findings

### ID: F1 / TITLE: `emit()` is reachable from model-influenced paths via `propose.py` / SEVERITY: CRITICAL / LOCATION: collaborator/propose.py:163 / CONCRETE INPUT OR BYPASS: A malicious model can propose a `net_post` action with `source="proposed"`, which gets governed as `HELD`. An attacker could then call `approve(session, proposal.decision)` directly (bypassing `approve_proposal`) to execute it. While the emission floor correctly requires `source=="host"` for autonomy, this path allows model-originated emissions to be executed at all, which violates the structural bar in `_candidate_from_response` that drops `net_post` proposals. However, the current code in `_candidate_from_response` correctly returns `None` for `net_post` proposals, so this attack is blocked. / WHETHER ANOTHER CHECK CATCHES IT: Yes, the structural bar in `_candidate_from_response` prevents `net_post` proposals from being created. / FIX: None needed; the existing check is sufficient.

### ID: F2 / TITLE: Canonical host confusion between auto-cap check and connect host / SEVERITY: HIGH / LOCATION: collaborator/governance.py:345 / CONCRETE INPUT OR BYPASS: An attacker could craft a URL with IDNA homographs or case variations that canonicalize differently in the auto-cap check versus the connect host. However, the code uses `egress.canonical_host()` consistently for both the capability check (`auto_host = ... directive.grants_capability(egress.EGRESS_AUTO_PREFIX + emit_host)`) and later in `execute_and_verify` for credential lookup and connection. / WHETHER ANOTHER CHECK CATCHES IT: Yes, the same `canonical_host` function is used throughout, ensuring consistency. / FIX: None needed; the implementation is consistent.

### ID: F3 / TITLE: Mutation of held.args between emit() and approve() breaks approved==sent / SEVERITY: MEDIUM / LOCATION: collaborator/loop.py:129 / CONCRETE INPUT OR BYPASS: The `approve()` function correctly snapshots `args = dict(decision.args)` before any checks, preventing TOCTOU attacks where a mutable args object could return different values on subsequent reads. The test `test_snapshot_reads_held_args_once_so_a_proxy_cannot_swap_the_wire` confirms this protection. / WHETHER ANOTHER CHECK CATCHES IT: Yes, the snapshot mechanism prevents this attack. / FIX: None needed; the implementation is correct.

### ID: F4 / TITLE: Credential leakage via response echo / SEVERITY: HIGH / LOCATION: collaborator/tools.py:328 / CONCRETE INPUT OR BYPASS: The `_redact_credential` function correctly redacts both the full Authorization header value and the bare token from the response body before it becomes audit-visible. The test `test_echoed_credential_is_redacted_from_output_and_summary` confirms this protection. / WHETHER ANOTHER CHECK CATCHES IT: Yes, the redaction mechanism prevents credential leakage. / FIX: None needed; the implementation is correct.

### ID: F5 / TITLE: Autonomous emission from non-enforced session / SEVERITY: CRITICAL / LOCATION: collaborator/governance.py:347 / CONCRETE INPUT OR BYPASS: The emission floor correctly checks `enforced(session)` as one of the required conditions for autonomy (`auto = (auto_host and host_directed_atr and enforced(session) and getattr(intent, "source", "") == "host" and leash == ACT_THEN_REPORT)`). The test `test_legacy_unsigned_session_cannot_auto_lift` confirms that legacy unsigned sessions cannot achieve autonomous emission. / WHETHER ANOTHER CHECK CATCHES IT: Yes, the `enforced(session)` check prevents this attack. / FIX: None needed; the implementation is correct.

### ID: F6 / TITLE: Unlisted net_post case neither emits nor silently no-ops / SEVERITY: MEDIUM / LOCATION: collaborator/governance.py:350 / CONCRETE INPUT OR BYPASS: The code correctly handles the unlisted net_post case by defaulting to `NOTIFY_ONLY` under enforcement (`leash_cap` returns `NOTIFY_ONLY` for unlisted tools when enforced). The test `test_autonomous_emit_without_leash_cap_notifies_loudly_not_silently` confirms that this results in a loud notification rather than a silent no-op. / WHETHER ANOTHER CHECK CATCHES IT: Yes, the `leash_cap` function and emission floor logic handle this correctly. / FIX: None needed; the implementation is correct.

### ID: F7 / TITLE: emit()/approve() raises instead of failing closed / SEVERITY: LOW / LOCATION: collaborator/egress.py:420 / CONCRETE INPUT OR BYPASS: The `post()` function correctly handles edge cases like lone surrogates (by returning a `FAILED` decision rather than raising) and other transport errors. The tests `test_lone_surrogate_body_autonomous_fails_not_raises` and `test_egress_backstop_downgrades_any_raise_to_failed` confirm this behavior. / WHETHER ANOTHER CHECK CATCHES IT: Yes, the egress functions are designed to never raise, and the governance layer handles any unexpected exceptions. / FIX: None needed; the implementation is correct.

### ID: F8 / TITLE: Retiring signed_leash_cap weakens existing guarantees / SEVERITY: LOW / LOCATION: collaborator/policycaps.py:167 / CONCRETE INPUT OR BYPASS: Removing `signed_leash_cap` was intentional to implement the "require both" rule. The change doesn't weaken existing guarantees for other tools like `web_fetch` GET or `run_command`, as they don't rely on the auto-lift logic that was specific to `net_post`. / WHETHER ANOTHER CHECK CATCHES IT: N/A, as this is an intentional change that doesn't affect other tools. / FIX: None needed; the change is correct per the design.

## Steelman

The PR correctly implements the "require both" rule for autonomous emissions by mandating both a per-host `net.post.auto:<host>` capability AND an explicit `net_post act_then_report` leash-cap. The code includes multiple defense-in-depth measures: requiring both `source=="host"` AND the keyword leash for autonomy, snapshotting args in `approve()` to prevent TOCTOU, redacting credentials from responses, and using consistent canonicalization throughout. The test suite thoroughly covers the red-team scenarios, including edge cases like IDNA homographs, credential echoing, and argument mutation.

## Verdict

SOUND. The implementation correctly enforces the "require both" rule while maintaining all existing guarantees. The highest-value fix already implemented is the dual requirement of `source=="host"` AND the keyword leash for autonomous emissions, which provides defense-in-depth against model-influenced paths.
