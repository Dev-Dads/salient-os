# Red-team (consumer, pass=general): deepseek/deepseek-v4-pro

_finish=length seconds=219.4 usage={'prompt_tokens': 25070, 'completion_tokens': 12000, 'total_tokens': 37070, 'cost': 0.020732075, 'is_byok': False, 'prompt_tokens_details': {'cached_tokens': 0, 'cache_write_tokens': 0, 'audio_tokens': 0, 'video_tokens': 0}, 'cost_details': {'upstream_inference_cost': 0.020732075, 'upstream_inference_prompt_cost': 0.010592075, 'upstream_inference_completions_cost': 0.01014}, 'completion_tokens_details': {'reasoning_tokens': 12000, 'image_tokens': 0, 'audio_tokens': 0}}_

We need to red-team the implementation for concrete defects under the stated guarantees. Focus on the consumer (`bounded_iterations` and related functions in `salience_observer.py`) and its call site in `agent/turn_context.py`. We need to find issues that were missed by two internal reviews. The context says the code reviewed already fixed certain things; we should not re-report those. We need to look for concrete trigger paths.

Let's read the code carefully.

**Guarantees list**:
1. FAIL-OPEN / NEVER-BREAK-THE-TURN: any failure, absence, deny-shaped directive, etc. MUST return `default` unchanged. Must never crash host turn, never return budget < 1. Hunt for path where exception escapes `except (Exception, SystemExit)` in any consumer path, or returns non-int or sub-1.
2. DENY-SHAPED GUARD (A5): treat hard-deny/malformed as absent (=> default), not re-derive.
3. CONSUMER, NOT DECIDER (Finding D): apply recorded, policy-clamped compute_budget verbatim, never re-clamped against default/config.
4. A3 — TURN N APPLIES TURN N-1: finalize-on-read must close prior window before reading, not read turn N's own or 2-turns-stale.
5. A4 — OPERATOR-BUDGET BINDING: finalize-on-read close binds policy floor to the caller-passed `default` (this turn's resolved budget). v0 echoes operator budget; flag docstring/config text that hides or overstates.
6. RESTART FALLBACK INTEGRITY (grok-F8): when in-memory cache empty, last budget recovered from disk must go through replay-verifying bus, not unverified; hunt for path returning unverified on-disk value or TOCTOU.
7. CONCURRENCY: lock escape, re-entrant re-acquire deadlock.
8. KILL SWITCHES / LEAK: `consume_compute` gate works, `_LAST_DIRECTIVE` freed on session close.

Then 6. HONESTY: docstring/config text overstates/misleads.
7. TEST HONESTY: mutation-blind tests.

Now attack.

First, examine `bounded_iterations` and its path.

- `bounded_iterations` calls `_consume_enabled()` which calls `salience_enabled()` and `_config_flag`. That can raise? `salience_enabled` already catches exceptions itself, but `_config_flag` is called inside `_consume_enabled` and that is inside `bounded_iterations` before the try-except? Actually `bounded_iterations` wraps everything in try-except (Exception, SystemExit). So that's ok. However, there's a note: `_config_flag` returns True/False and catches (Exception, SystemExit) around `read_raw_config_readonly()`? Wait, let's check `_config_flag` code:

```python
def _config_flag(key: str, default: bool) -> bool:
    try:
        from hermes_cli.config import read_raw_config_readonly
        cfg = read_raw_config_readonly() or {}
    except (Exception, SystemExit):  # a config helper that sys.exit()s must not crash the host
        return False
    ...
```

So `_config_flag` catches Exception and SystemExit, so it won't propagate. That seems safe.

But there is a potential issue: `_consume_enabled` calls `salience_enabled()` which calls `_config_flag("enabled", True)`. That is fine. However, what about `_operator_budget()`? It is called inside `_close_locked` which is called from `_resolve_bounded` under lock, and `_operator_budget` also tries to read config with try-except, so it's safe. But `_close_locked` also uses `get_hermes_home` via `_bus_for`, which itself might call `get_hermes_home`. Look at `_bus_for`:

```python
def _bus_for(session_id: str):
    bus = _BUSES.get(session_id)
    if bus is None:
        from pathlib import Path
        from hermes_constants import get_hermes_home
        directory = Path(get_hermes_home()) / "salience"
        directory.mkdir(parents=True, exist_ok=True)
        path = directory / (_session_hash(session_id) + ".jsonl")
        bus = SalienceBus(str(path))
        _BUSES[session_id] = bus
    return bus
```

`get_hermes_home()` is a host API that could raise SystemExit? The comment in `_config_flag` warns that a config helper might sys.exit(). Similarly `get_hermes_home` might be a simple constant access, but it's not guarded. However, `_bus_for` is called from `_close_locked`, which is called inside `_resolve_bounded` under `try-except` in `bounded_iterations`. So an exception in `get_hermes_home()` (including SystemExit) would be caught by `bounded_iterations`'s except clause and return default. But we need to check if `_close_locked` is called elsewhere that might not be guarded? `_close_locked` is called from `_close_session` and `_open_window` as well. In those paths, the call is inside `observe_lifecycle`, which has its own `except (Exception, SystemExit)`. So `get_hermes_home` raising SystemExit would be caught there too. So it might be ok.

But here's a nuance: `_close_locked` also calls `_operator_budget()` (if budget is None, which is the case for the produce path, but when called from the consumer it passes `budget=floor`). Actually the consumer's finalize-on-read passes `budget=floor` which is an int, so `_operator_budget` is not called. In the produce path (turn rollover, session end), `budget=None` and it calls `_operator_budget`, but that is inside `observe_lifecycle` which swallows exceptions. So it seems safe.

Now, look for a path where `bounded_iterations` can return a value < 1 or a non-int. The function returns `default` if anything fails, but `default` is the caller's value. The guarantee says "MUST return `default` unchanged" on failure. But it also says "must NEVER return a budget < 1 (that would brick the agent at max_iterations 0)". The consumer is supposed to return the recorded budget only if it's a valid positive int. `_directive_budget` enforces that. So if the recorded budget is valid, it returns it; else returns default. However, what if `default` is already < 1? That's outside the consumer's control; the guarantee likely means the consumer must not *itself* return a value lower than 1 unless the caller's default was already that. It says "MUST return `default` unchanged" for failure, but also "never return a budget < 1 (that would brick the agent)". The combination is ambiguous: if the caller passes a default of 0 (or negative), the function will return 0 (since it returns `default` unchanged). That could brick the agent. The function checks `if not isinstance(default, int) or isinstance(default, bool): return default` early, so a non-int, bool, etc., are passed through unchanged. For an int 0, it will proceed, and the budget from directive is applied only if it's valid and >=1, else it returns default (0). So the consumer could return 0 if the caller's default is 0. But is that a bug? The host's max_iterations should never be 0; but if it is, the consumer won't fix it. The guarantee says "never return a budget < 1 (that would brick the agent)". So perhaps the consumer SHOULD guard against returning a budget < 1 even if the default is < 1, by clamping to 1? Or perhaps the consumer should treat a default < 1 as invalid and return some safe minimum? The requirement says: "ANY failure, absence, deny-shaped directive, switched-off subsystem, kill-switch, missing session, or exception [...] MUST return `default` unchanged. The consumer must NEVER crash the host turn and must NEVER return a budget < 1 (that would brick the agent at max_iterations 0)." This implies that even if the default is < 1, the consumer should return a value >= 1? Or it says it must never return a budget < 1, but if default is < 1, returning default unchanged contradicts that. I think the intent is that the consumer should not cause max_iterations to become 0 due to a directive, but if the operator set max_iterations to 0, that's an operator problem. But the phrasing "MUST NEVER return a budget < 1" is absolute. Let's see if any code in the consumer could return < 1 if default is 1. No, it returns default only, which is the caller's value. So if default is 1, it's fine. But if the caller's default is 0 (maybe due to a config error), the consumer would return 0, and the agent would be bricked. But that's not the consumer's fault; the function's docstring says "Fails OPEN to `default` on EVERYTHING", so that's consistent. However, the guarantee explicitly says "must NEVER return a budget < 1 (that would brick the agent at max_iterations 0)." That could be interpreted as the consumer's absolute promise. If the caller passes 0, the consumer should perhaps return 1? But then it would be overriding the operator's choice, which is undesirable. But it's a safety guarantee. The test "test_non_int_default_returned_unchanged" tests for non-int, but doesn't test for 0 default. I'll note this as a potential violation of the guarantee, but it's arguable. Since the host's max_iterations is always a positive integer in practice, maybe it's fine. But we need to be rigorous.

Let's look at the call site: `agent.max_iterations = _salience_observer.bounded_iterations(agent.session_id or "", default=agent.max_iterations)`. `agent.max_iterations` is presumably an int > 0 (default 500). So in production, default will never be 0. So the absolute guarantee that it never returns <1 is met in practice because default itself is >0. But if we are evaluating the code against the stated guarantee strictly, we could say the function does not explicitly ensure >=1, but the guarantee is about the consumer's effect on the agent, and the caller's value is under the host's control, so maybe not a bug. I'll keep this as a LOW finding if at all.

Now, focus on FAIL-OPEN: any path where consumer crashes host turn or propagates exception. The consumer's entire body is wrapped in try-except (Exception, SystemExit) and returns default. But there's a call to `_resolve_bounded`, which holds `_LOCK`. Inside that, `_close_locked` is called and it calls `_bus_for` which calls `get_hermes_home` — which might raise SystemExit, but that is caught by the outer try. So no propagation.

But what about `_close_locked` calling `_operator_budget()`? Not in consumer path. What about `_ensure_template_valid()`? It is inside the `with _LOCK`: and it calls `issue_policy`, `verify_policy`, etc. Could those raise BaseException (not Exception)? They could only raise Exception. So that's fine.

What about `_budget_from_disk`? It opens a file with `open(path, encoding="utf-8")` inside a `with` and does `json.loads`. Could that raise KeyboardInterrupt? No, that's not caught; but KeyboardInterrupt is explicitly not caught anywhere (the requirement says "KeyboardInterrupt is intentionally NOT caught anywhere" in `observe_lifecycle` comment; in `bounded_iterations` the except does catch BaseException? It catches `(Exception, SystemExit)` only; KeyboardInterrupt is NOT caught. So if a KeyboardInterrupt occurs while reading the file, it will propagate and crash the turn. That's a violation of "ANY failure ... MUST return default" if you consider KeyboardInterrupt as a failure/exception. However, the guarantee says "The consumer must never crash the host turn", and KeyboardInterrupt could crash the host if not caught. But the host likely has its own signal handling. The design says KeyboardInterrupt is deliberately not caught to allow user interruption. So perhaps the guarantee is about unexpected failures, not signals. But we need to check: is there a path where a slow file read yields a KeyboardInterrupt that would kill the host? In a normal environment, the host will handle it, but the consumer not catching it could cause the entire program to exit via SystemExit or KeyboardInterrupt. The requirement says "ANY failure, absence, deny-shaped directive, switched-off subsystem, kill-switch, missing session, or exception (incl. SystemExit from a host API) MUST return `default` unchanged." KeyboardInterrupt is not SystemExit; it's BaseException. But the spec says "exception (incl. SystemExit)", implying they want to catch all exceptions that could escape, but they explicitly mention KeyboardInterrupt is not caught elsewhere. The consumer docstring says "Fails OPEN to `default` on EVERYTHING". The comment in `observe_lifecycle` says "KeyboardInterrupt is deliberately NOT caught: the user's interrupt must reach the host." So for the consumer, should it also not catch KeyboardInterrupt? The spec says "MUST return `default` unchanged" for any failure; a KeyboardInterrupt is a user interruption, not a failure of the subsystem. It might be acceptable for the consumer to allow it through. I'd say it's not a defect because it's by design. But we can note that if a KeyboardInterrupt occurs during `_budget_from_disk` (reading a large file?), it could crash the turn, but that's the user's choice. So no issue.

Now, DENY-SHAPED GUARD: `_directive_budget` implementation checks for blank subject/policy_id and non-int/bool/sub-1 budget. That seems correct.

Now, CONSUMER NOT DECIDER: it just returns the budget verbatim. The only place that could re-clamp is if somewhere the recorded budget is not applied directly. `bounded_iterations` does `return budget if budget is not None else default`. So no re-clamp. Good.

A3: TURN N APPLIES TURN N-1. This is done via `_resolve_bounded` which finds the open window for session_id (which should be the prior turn's) and closes it. The call site is at line ~491 in `build_turn_context`, BEFORE `pre_llm_call` opens the current turn's window at line ~1054. So at that point, the open window should be turn N-1's if it hasn't been closed yet. But the window-open logic (`_open_window`) closes the previous window when a new turn ID arrives: 
```python
if current is not None and not current.closed and current.turn_id != turn_id:
    _close_locked(current)
```
So when `pre_llm_call` is called for turn N, it will close turn N-1's window if not already closed. But the consumer's `_resolve_bounded` runs BEFORE that, so it might find the window still open for N-1, and close it. That's correct. However, there's a potential off-by-one: What if the consumer runs, closes turn N-1's window, and then later `pre_llm_call` for turn N also tries to close it again? `_close_locked` is idempotent, so no issue. But could there be a case where `pre_llm_call` for turn N runs BEFORE the consumer? The call order in `build_turn_context` shows: first the consumer at :491, then much later `pre_llm_call` at :1054. So that's fine.

But what if the previous turn's window was never opened? For example, the first turn: there is no prior window. `_resolve_bounded` will find no window, no `_LAST_DIRECTIVE`, and will fall to disk (which might not exist). So it returns default. That's fine.

Potential bug: The consumer closes the prior window and then reads `_LAST_DIRECTIVE` from the cache. But `_LAST_DIRECTIVE` is set by `_close_locked` only on successful emit. So if the close was already done (by a prior call), `_LAST_DIRECTIVE` would still be there. So it's fine.

Another A3 scenario: If the consumer is called multiple times for the same turn (maybe re-invocation?), it would close the prior window again (idempotent) and read the cached directive again, which is the correct one. No issue.

Now, RESTART FALLBACK INTEGRITY: `_budget_from_disk` is called only if `session_id not in _BUSES`. It constructs the bus via `_bus_for(session_id)`, which for a new bus with an existing file calls `SalienceBus.__init__` which does `_replay` and verifies the chain. So that's verifying. Then it manually opens the file again and scans for the last directive line to get the subject, then calls `bus.directives_for(last_subject)` to get the verified directives. Is there a TOCTOU? The file is opened after the bus is constructed and replayed. However, the bus's `_replay` reads the entire file into memory; the subsequent manual `open(path)` reads the file again. But the bus's internal state is based on the file at the time of `_replay`. If another process appends a directive after `_bus_for` is called but before our manual open, the manual open could see an extra directive line, and `last_subject` could be a new subject not yet in the bus's verified directives. But `_bus_for` is called with the same file path, and the bus is cached now (`_BUSES[session_id]` is set), so the bus's directives are from its replay. The manual scan could pick up a newer directive line that was appended after the bus was constructed, and the `bus.directives_for(last_subject)` would not find it (since the bus's state doesn't have it), so it would fallback to returning None? Actually `directives[-1]` will raise IndexError if directives list is empty, but `_directive_budget` handles the case. Let's read `_budget_from_disk`:

```python
def _budget_from_disk(session_id: str) -> "int | None":
    if session_id in _BUSES:
        return None
    from pathlib import Path
    from hermes_constants import get_hermes_home
    path = Path(get_hermes_home()) / "salience" / (_session_hash(session_id) + ".jsonl")
    if not path.exists():
        return None
    bus = _bus_for(session_id)  # constructs ⇒ replay + verify (raises on a corrupt chain)
    last_subject = None
    with open(path, encoding="utf-8") as fh:
        for raw in fh:
            line = raw.strip()
            if not line:
                continue
            entry = json.loads(line)
            if entry.get("kind") == "directive":
                last_subject = (entry.get("payload") or {}).get("subject")
    if not last_subject:
        return None
    directives = bus.directives_for(last_subject)  # verified copies, oldest first
    return _directive_budget(directives[-1]) if directives else None
```

The potential issues:
1. The bus is constructed and replayed from the same file. Then we open the file again and read it. There is no lock preventing concurrent writes to the file. In a multi-process scenario (which is not the case; the contract is single-threaded, but the bus is a file, could be appended by another process? Probably not; the host is a single process. So no real concurrency.
2. However, there's a subtle bug: the bus's `_replay` might raise an error if the file is corrupt; but we only call `_budget_from_disk` if session_id not in _BUSES. If the bus raises an exception during construction, that exception will propagate up to `bounded_iterations` which catches it and returns default. That's fine.
3. The manual file read uses `json.loads` which could fail on malformed lines, but the except in `_budget_from_disk`? There's no try-except inside `_budget_from_disk`. So if any line is not valid JSON, the `json.loads` will raise an exception, which will propagate out of `_budget_from_disk`, up through `_resolve_bounded` (called under lock, but the lock is released if exception? Actually `_resolve_bounded` uses `with _LOCK:` so the lock is a context manager; an exception will properly release the lock). Then `bounded_iterations` catches it. So that's ok.
4. The more subtle issue: the bus's `_replay` verifies the chain, but the final step of the fallback (the manual scan) could pick a subject from a directive that is beyond the verified head? As mentioned, after `_replay`, the file is closed; subsequent appends won't be in the bus's memory. If someone appends after the bus was constructed, the manual scan would see the new directive, but the bus won't have it, so `directives_for` would return nothing, and `_budget_from_disk` would return None, falling back to default. That's safe. But what if the file is truncated between bus construction and manual open? The manual open would then see fewer lines, and the last_subject could be from a line that the bus verified and has in memory. But because the file was truncated, the manual scan might skip the tail that the bus already has, but if the truncation removed the last directive entry, the manual scan would find an earlier directive, get its subject, then `bus.directives_for` would return directives for that subject (which are still in the bus). That would return a budget from a *stale* directive (not the last one), because the last one was truncated. However, truncation would cause the bus's replay to fail because the file is missing the tail that was there? Wait, the bus is constructed from the current file at the time of `_replay`. If the file is truncated AFTER the bus is constructed, the bus's state still has the full chain from the original file. So it's possible that the file's tail is deleted, the manual scan picks an earlier directive, and we return a budget from an earlier turn. This violates the integrity: we might return a stale budget, not the last. This is a TOCTOU vulnerability: the file state can change between bus construction and manual read, bypassing the chain verification for the tail? But the chain verification already verified the file that was present at construction. If someone truncates the file after verification, we might then read a stale directive because we're scanning the truncated file. Yet the guarantee says "restart fallback integrity: must go through replay-verifying bus so a corrupt/tampered tail raises => default". In this scenario, the tail corruption is not detected because the bus already accepted the old full file. The manual scan of a truncated file could give a subject from an earlier directive, and the bus has that directive in memory, so it returns a budget from an earlier turn. This could happen if the file is tampered with between bus construction and the manual scan. But is this a realistic attack? In a single-process system, no external process should be modifying the file. However, if the file is tampered with externally (maybe an operator deletes the tail), then this could happen. But the guarantee says it must not trust an unverified value. Here, the value from the earlier directive is verified (it's in the bus). But it's stale. So the consumer would apply a budget from two turns ago, violating A3 staleness. Is that a concrete defect? The sequence: session has three turns, directives for u1 (budget 1), u2 (budget 2), u3 (budget 3). All are written. Process restarts. Before restart, the file has all three directives. On restart, consumer calls `_budget_from_disk`: `_bus_for` constructs bus, replays all three, verifies chain. Then manual open of file: some external actor deletes the last line (u3). Now manual scan finds u2 as last_subject. `bus.directives_for("u2")` returns the directive for u2, which is budget 2. Consumer returns budget 2, which is stale (should be budget 3, the last one). So the consumer applies stale budget. This violates A3 and the restart integrity guarantee, because the tampered tail (deletion) caused a wrong budget to be applied without error. The guarantee states: "This MUST go through the replay-verifying SalienceBus so a corrupt/tampered tail RAISES (=> default), never feeding an unverified value." This scenario bypasses that because the corrupt/tampered tail is the one that was deleted, not corrupted; the bus's verify succeeded on the original file, but then subsequent deletion of tail lines goes undetected because the manual scan uses the file after deletion. So the replay-verifying bus does not protect against post-verification file modification. The bus itself when it replayed saw the full file; it's not called again. So indeed, there is a TOCTOU where the file can be truncated after bus construction but before the manual read, leading to a stale budget.

Is this within the scope of "non-malicious corruption"? The ADR says tail truncation across reopen is accepted? Wait, the ADR 0001 says "tail truncation across reopen is accepted" for the bus's internal verify_chain. But the context in PR-H2 says the already-merged finding (c) was that a corrupt-tail test was caught by the observer's own json.loads rather than the bus replay-verify, and they added a non-tail wrong-hash test. So the restart fallback's integrity guarantee is specifically "grok-F8" which says the consumer must go through the replay-verifying bus so a corrupt/tampered tail RAISES. The scenario of tail truncation after bus construction might be a known exclusion, because the bus's replay would not detect it anyway. But the guarantee is about feeding an unverified value. In the scenario, the value from u2 is verified (it's in the bus). So it's not unverified, just stale. So maybe it's not a violation of grok-F8. However, the guarantee says "corrupt/tampered tail RAISES (=> default)". If the tail is simply truncated, the bus would not raise anyway because the replay sees a valid chain from the truncated file. But in this case, the file was truncated AFTER replay, so the bus's state still has the old full chain. If we then call `verify_chain()` on the bus, it would still verify correctly because it's based on in-memory entries, not the file. So the bus thinks the chain is intact, but the file is truncated. The consumer then reads the subject from the truncated file and uses the bus's directives to get a value, which yields a stale one. This seems like a weakness: the fallback mechanism is not robust against file tampering that occurs after the bus is loaded. But is that a realistic threat? Probably not in production, but it's a concrete defect because the restart fallback integrity is supposed to ensure that the consumer uses a verified, current last directive. The current code reads the file again, which could be inconsistent with the bus's verified chain. A safer approach would be to use the bus's own last directive subject without re-reading the file. The bus already has the directives list; the `_replay` method populates `_directives` list, and `directives_for` can give them by subject. So why re-read the file? The comment says "the subject comes from the now-verified file's last directive line" but that's the manual scan. The bus's replay already verified the file's content. But to get the last directive's subject, we could simply use the last entry in `bus._directives` (which is a private attribute, but `directives_for` is the public API). We could call `bus.directives_for(...)` on the last subject from the bus's own parsed entries, avoiding a second file read. The current code's manual scan introduces the inconsistency. So I'd say this is a TOCTOU vulnerability: the consumer reads the file after the bus, bypassing the bus's verified view. So this could be a HIGH or MEDIUM finding.

Let's check if the bus provides a way to get the last directive subject without scanning the file. `directives_for` requires a subject. To get the last subject, we could iterate over `bus._directives` (private). But perhaps we can use `bus.directives_for` with all subjects? Not straightforward. However, the code could be modified to avoid this second read. This is a real defect: the restart fallback re-reads the JSONL file, potentially using a truncated view, and returns a stale budget while the bus's internal state has the correct chain. This violates the guarantee that the last budget is recovered from the session JSONL via the replay-verifying bus, because the bus's verification is not used to obtain the last budget; it's used only to later verify the subject's directives, but the subject itself is taken from an unverified re-read that could be stale. So a corrupted (truncated) tail after bus construction would not raise; instead, it returns a budget from the truncated file's last directive, which might be stale. That's a concrete defect.

But note: the bus's replay may raise an error if the file is corrupt (invalid hash), but if the file is simply truncated (missing lines), the bus's replay will succeed as long as the remaining lines are a valid chain. So a truncation that removes the last directive line(s) would not raise. So the bus replay would succeed, but then the manual scan reads the now-truncated file and picks a stale subject. This is a concrete finding.

Now, does `_budget_from_disk` ever get called when there is already a cached bus? It checks `session_id not in _BUSES`, so it only runs when no bus is cached. That's the cold start. So this TOCTOU could happen when the process starts, the file is intact, then external process truncates the file before the manual scan. Unlikely but possible.

Now, concurrency: All registry access under `_LOCK`. But `_resolve_bounded` holds `_LOCK` for the whole function, which includes `_close_locked` and `_budget_from_disk`. `_close_locked` calls `_bus_for` which can call `get_hermes_home` and `mkdir`. These are I/O operations that could potentially be slow. Holding the lock during I/O is not ideal but not a re-entrant deadlock. Are there any re-entrant calls? `_bus_for` does not acquire `_LOCK`. `_budget_from_disk` opens the file; that's fine. So no deadlock.

But there is a potential lock escape? If an exception occurs inside `_resolve_bounded`, the `with _LOCK:` context manager will release the lock. So no lock leak.

Now, check for resource leaks: `_LAST_DIRECTIVE` is popped in `_close_session`, so no per-session leak.

Now, KILL SWITCH: `consume_compute` gate is checked at top. Works.

Now, HONESTY: docstring says "the directive always echoes the operator's own budget — the consumer is behavior-preserving by construction" and mentions that moving the budget requires mapping a facet and widening the window. That's honest. But there might be overstatement: the module docstring says "The one *consumer* — ``bounded_iterations`` (PR-H2, the compute-budget knob) — is a separate, explicitly-gated entry point with its own kill switch (``salience.consume_compute``); it applies the recorded decision and fails open to the caller's value." That's accurate. The config example text says "the recorded compute budget bounds the next turn's iteration budget, applied between turns. In this v0 config the directive echoes the operator's own configured budget, so consumption is behavior-preserving". It's clear. No overclaiming.

But perhaps the statement in module docstring: "It is wired live but INERT in v0: the produce policy pins ``min_budget == max_budget == operator budget`` and ATTENTION is unmapped, so the directive always echoes the operator's own budget — the consumer is behavior-preserving by construction." That is accurate.

TEST HONESTY: we need to find a test that would stay GREEN if the corresponding production line were sabotaged. Look at tests.

`test_restart_nontail_tamper_fails_closed` is supposed to test that a non-tail hash tampering raises and falls back. That test modifies the file's hash for a non-tail directive. Then calls `bounded_iterations`. It expects 10 (default). If we sabotage the replay integrity check (e.g., comment out the hash check in `_replay`), would the test still pass? It tests exactly that scenario; if the integrity check is removed, the tampered file would be accepted, and the bus would read the tampered directive with budget 7 (the tail's budget), so `bounded_iterations` would return 7, not 10. So test would fail (red). So it's non-vacuous.

`test_restart_corrupt_tail_fails_closed_to_default` adds invalid JSON at the end, causing json.loads in the manual scan to raise? Actually the corrupt tail test appends "}{ this is not valid json", then the bus replay would fail because the line is not valid JSON, raising ValueError. The test expects default (10). If we sabotage the `_replay` to skip that check, the bus would still fail because it would try to parse that line? `_replay` uses `json.loads(line)` and then checks the hash; if the line is not valid JSON, json.loads raises, which is caught by `except Exception: intact = False` and then raises ValueError. So if we sabotage the replay to not raise on corrupt line, the test would fail. So that's covered.

But there is test `test_restart_fallback_skipped_when_bus_cached`. That test manually calls `_bus_for` to cache the bus, then calls `bounded_iterations`. It expects default. This tests that the fallback is skipped. If we remove the `session_id in _BUSES` guard, the test would go into `_budget_from_disk` and attempt to recover from disk (the file still has the directive with budget 7). Since the bus is already cached, the guard would be bypassed, and the code would proceed to `_bus_for(session_id)` which would return the cached bus, then manual open (file exists) and it would read the last directive subject and get the budget 7, returning 7, not 10. So test would red. So it's guarded.

Now, what about the A3 test `test_three_turns_read_prior_not_stale`? It ensures that the third turn reads the second turn's budget. If we sabotage the finalize-on-read (by removing the `_close_locked` call inside `_resolve_bounded`), the test would fail because the third turn would return 20 or something? Let's see: The sequence: turn1 calls bounded_iterations (no prior window), returns 10. Then `_open("s","u1")`, `_record_write`. Then turn2 calls bounded_iterations (finalizes u1 with default 20). If finalize-on-read is sabotaged (i.e., no close of u1), then `_LAST_DIRECTIVE` remains empty, so it falls back to disk (since no bus cached? Actually in the test, after turn1's open and write, there is no close, so the directive for u1 is not yet written to disk? Wait, the test does not call `_close_locked` explicitly; the finalize-on-read is supposed to close u1. If we comment out the close, u1 stays open and its directive is not emitted to bus nor cached. So when `bounded_iterations("s", 20)` runs, it won't find `_LAST_DIRECTIVE`, and it goes to `_budget_from_disk`. But is the bus created? It would create a bus with `_bus_for(session_id)`, and since there is no file (none created yet because no close happened), `path.exists()` is False, so it returns None, and `bounded_iterations` returns default 20. So turn2 returns 20, not an error, but then the third turn's call: turn2 after its bounded_iterations, the test then does `_open("s","u2")` which would trigger the rollover close in `_open_window` because current window is u1 (still open) and turn_id != u2, so `_close_locked(current)` would close u1. That close will emit the directive for u1 with the operator budget (default 25, because the produce path's rollover close uses `budget=None` and calls `_operator_budget()`). So after that, u1 directive is on disk and in `_LAST_DIRECTIVE`. Then `_record_write("s","u2")`. Then turn3 calls bounded_iterations with default 30. It will find a window for u2 open? Actually u2 is the current window, open. It will finalize that window (u2) with budget=30, emitting its directive, caching it. Then it returns that budget (30). So turn3 would still read the directive of turn2 because the finalize-on-read now applies to u2 (the prior turn? Actually u2 is the prior turn for turn3? The test opens u2 after turn2's read, so for turn3, the prior turn is u2. So it works, even though u1 was not closed on time? Let's trace carefully:

1. applied1 = bounded_iterations("s",10) -> no prior window, no cache, no file, returns 10.
2. _open("s","u1") -> window u1 opened.
3. _record_write("s","u1") -> signals recorded.
4. applied2 = bounded_iterations("s",20) -> finalize-on-read should close u1. If sabotaged, u1 remains open. No _LAST_DIRECTIVE, falls to disk. Disk: file not exists (since no bus yet), so returns 20. So applied2 = 20. (expected 20, but actually u1's directive was not recorded yet; it would be recorded later when u1 is closed. In the proper sequence, applied2 = 20 because the directive of u1 is emitted with floor 20.)
5. _open("s","u2") -> sees existing window u1 not closed and turn_id different, so rollover closes u1 with budget=None (operator budget 25). That emits u1 directive with budget 25 (since _operator_budget() returns 25 from config? The test config is `{"agent": {"max_iterations": 7}}`? Let's see the test setup for three_turns: the fixture `home` uses `_use_config(monkeypatch, tmp_path, {"salience": {"enabled": True}}, gate=True)`, so no agent max_iterations config, so _operator_budget() returns _DEFAULT_BUDGET=25. So u1's directive gets budget 25.
6. _record_write("s","u2") -> nothing.
7. applied3 = bounded_iterations("s",30) -> finalize-on-read closes u2 with budget=30, emits directive for u2 with budget 30, caches it, returns 30. So applied3 = 30. The test expects (10,20,30). So even with finalize-on-read removed, applied2 would be 20 (still matches) and applied3 would be 30. So the test would still pass? Let's check: with finalize-on-read removed, the cache `_LAST_DIRECTIVE` would only be set when windows are closed by rollover or session close. After step 5, u1 is closed (by rollover), so `_LAST_DIRECTIVE` contains u1's directive (budget 25). But at step 4, `bounded_iterations` didn't close u1, so it didn't set `_LAST_DIRECTIVE`. So at step 7, when bounded_iterations runs, it sees open window u2, closes it, and caches its directive (budget 30). It then reads `_LAST_DIRECTIVE` and returns 30. That's the same as the correct behavior (where finalize-on-read in step 4 would have closed u1 with budget=20 and cached it, and then step 7 would close u2 with budget=30 and return 30; but step 2's `applied2` would be 20 because it reads u1's cached directive, which would have budget 20. In the sabotaged scenario, applied2 is 20 because it fell back to default, not because of the directive. So the output tuple (10,20,30) is identical. So the test would remain green if we remove the finalize-on-read from `_resolve_bounded`! That's a huge test honesty issue. The test is designed to check that turn 3 reads turn 2's directive, but the expected values are based on the defaults passed to bounded_iterations, which happen to match the budgets if finalize-on-read worked correctly. In the sabotaged scenario, the budgets still work out because the defaults are the same as the finalize-on-read floor. The test doesn't actually verify that the directive's budget was applied (they could all be defaults). The test comment says: "Distinct per-turn defaults make the A3 property observable: turn 3 must read turn 2's window (30), not the stale turn-1 directive. Deleting finalize-on-read makes turn 3 read u1 ... at the operator budget (25) so turn 3 returns 25, not 30, and reds." But in our trace, without finalize-on-read, turn3 does not read u1; it reads u2, which it just closed, and the budget is 30 (the default), so it returns 30. The assumption in the comment that turn3 would read u1 seems incorrect because `_open("s","u2")` triggers rollover close of u1, but it does not set `_LAST_DIRECTIVE`? Actually rollover close in `_open_window` calls `_close_locked(current)` which does set `_LAST_DIRECTIVE`. So after step 5, `_LAST_DIRECTIVE` contains u1 directive with budget 25 (operator budget). Then when turn3's bounded_iterations runs, it sees open window u2 (still open because step 6 only recorded, didn't close), closes it, and sets `_LAST_DIRECTIVE` for u2 with budget 30, overwriting the previous entry (since session_id same). So it returns 30. So indeed, without finalize-on-read, the result is still (10,20,30). The test is not sensitive to the removal of finalize-on-read. Therefore it's a mutation-blind test. The comment says if you delete finalize-on-read, turn3 returns 25, but that's based on a misunderstanding of how the code works. In reality, `_LAST_DIRECTIVE` gets overwritten by the finalize-on-read inside the consumer for turn3, so it still gets 30. So the test does not guard A3. That's a serious test honesty issue (maybe HIGH or MEDIUM). 

We need to verify if the production code's `_resolve_bounded` always closes the prior window if open, which in the test's step 5, the window u1 was already closed by rollover before turn3's call, so `_resolve_bounded` finds no open window and doesn't close anything. Then it reads `_LAST_DIRECTIVE` which was set to u2's directive by the rollover? Wait, after step 5, u1 is closed, u2 is open. Then step 6 records signals. Step 7 calls `_resolve_bounded(session_id, 30)`. It finds window u2 open and not closed, so it closes it with budget=30 (finalize-on-read). That emits u2's directive with budget 30 and sets `_LAST_DIRECTIVE` to that directive. Then it reads `_LAST_DIRECTIVE` and returns budget 30. So the finalize-on-read for turn3 is what sets the directive for u2 and returns it. If we remove that finalize-on-read, `_resolve_bounded` would not close u2, and it would read `_LAST_DIRECTIVE` which at that point still contains u1's directive (budget 25). Then it would return 25. So turn3 would return 25, not 30. So the test WOULD red if finalize-on-read is removed. Let's re-evaluate: Without the finalize-on-read, the close of u2 does not happen, so `_LAST_DIRECTIVE` is whatever was there before, which is u1's directive from the rollover close at step 5. So applied3 would be 25. The test expected 30, so it would fail. So the test IS sensitive to the removal of finalize-on-read. Wait, earlier I thought rollover close of u2 doesn't happen until a subsequent open. In the test, after turn3's bounded_iterations, there is no further `_open` call. So u2 remains open, and `_LAST_DIRECTIVE` stays as u1's. So without finalize-on-read, turn3 would read u1's budget (25) and return 25. So the test would fail. Good, the test does catch that. So it's not mutation-blind. My earlier trace had a mistake: I assumed that `_open("s","u2")` sets `_LAST_DIRECTIVE` to u2's directive, but that's not true; opening a new window does not close the current one with its own signals; it closes the previous one (u1) with its signals. So after step 5, `_LAST_DIRECTIVE` contains u1's directive. Then `_record_write("s","u2")` just adds signals to u2 window. Then step 7: without finalize-on-read, `_resolve_bounded` sees u2 open, but doesn't close it, and reads `_LAST_DIRECTIVE` which still holds u1. So returns 25. So the test would fail. So it's okay.

But there is another potential test honesty issue: `test_finalize_on_read_closes_prior_window` opens a window u1, records a signal, then calls bounded_iterations with default 10. It asserts that the window u1 is closed and directive is on the bus and applied=10. If we remove the finalize-on-read, window u1 would remain open, so `so._WINDOWS["s"].closed is True` would fail. So that's fine.

Now, look for any path where `_budget_from_disk` returns a value without verification. We already found the TOCTOU.

Now, another potential issue: In `_budget_from_disk`, the bus is constructed via `_bus_for(session_id)`, which also caches it in `_BUSES`. That means after `_budget_from_disk` returns, the bus is now cached. But the consumer may later call `_resolve_bounded` again with the same session (perhaps if the process restarts and the same session is reused? unlikely). But the bus is now cached, and `_LAST_DIRECTIVE` is not set. In a subsequent turn, `_resolve_bounded` will see `session_id in _BUSES` (true) and skip disk, but `_LAST_DIRECTIVE` is empty, so it returns None, leading to default. That's okay. The bus being cached doesn't cause a problem.

Now, consider the scenario where the consumer is called for a session where a window is still open from a previous turn, but no directive was recorded yet. The finalize-on-read will close it and emit a directive. That's fine.

Now, the call site: `agent.max_iterations = _salience_observer.bounded_iterations(agent.session_id or "", default=agent.max_iterations)`. The test `test_call_site_precedes_budget_rebuild` verifies that the call line is before the IterationBudget rebuild and that no reassignment happens in between. That's structural but doesn't verify that the call is actually executed. It's a source code check, which is okay for regression.

Now, about the lock and `_operator_budget`. `_close_locked` when called from consumer passes `budget=floor`, so it doesn't call `_operator_budget`. But when called from produce path (rollover), it calls `_operator_budget` while under lock. That's okay because `_operator_budget` is safe under lock (it accesses the global cache). But note that `_operator_budget` itself does not acquire the lock; it relies on the caller holding it. This is documented: "It is only ever called from _close_locked while _LOCK is held, so the module-global cache needs no extra synchronization". That's correct.

Now, check for re-entrant lock: `_resolve_bounded` holds the lock, then calls `_close_locked` which calls `_bus_for`. `_bus_for` doesn't acquire lock. So no re-entrant.

Now, test `test_consume_kill_switch_leaves_budget_and_window_untouched` verifies that when `consume_compute` is False, the consumer does not finalize the window, and default is returned. That's okay.

Now, any other failure modes? Look at the `_budget_from_disk` using `bus.directives_for(last_subject)`. If the last subject's directives list is empty (shouldn't be, because we just found a directive entry with that subject), returns None. That's fine.

Now, consider a race condition if the consumer is called concurrently from multiple threads? The lock serializes, but could the consumer be called for the same session from different threads? The host is single-threaded for turn processing, so not an issue.

Now, about the `_LAST_DIRECTIVE` cache: when the consumer calls `_resolve_bounded` and closes a window, it sets `_LAST_DIRECTIVE` with the directive. This directive has the subject of the closed window. Later, if another turn uses the same session, it will read that `_LAST_DIRECTIVE`. But note that the subject includes the turn id. So the directive's subject is for the closed turn, e.g., based on turn N-1's turn_id. That's fine: turn N applies the directive from N-1, the subject reflects N-1's turn. So the subject is unique per turn; no collision.

Now, check the `_close_locked` function: it calls `_bus_for(window.session_id).emit(directive)`. The bus's `emit` checks directive type and shape, and writes to file. Could it raise an exception? It could raise TypeError if directive is not valid, but we've just produced a directive from `interpret`, so it should be valid. But if it raises, the try-except catches it and logs, and does NOT set `_LAST_DIRECTIVE` (since it only sets after successful emit). So a failed emit means no cached directive, and the consumer won't see it. That's correct.

Now, I'll write findings.

Potential findings so far:
1. **TOCTOU in restart fallback** (HIGH): `_budget_from_disk` re-reads file after bus construction, enabling stale budget if file truncated between bus construction and manual scan. Concrete trigger: cold restart, file truncated after bus replay but before manual scan.
2. **Missing guard in `_budget_from_disk` against non-JSON lines**? It uses `json.loads` without try-except, but that's intentional because exception propagates and is caught by outer handler, which returns default. So not a bug.
3. **`bounded_iterations` does not ensure returned budget >=1 if default is 0**? Could be considered a violation, but default is always positive. I'd still note as LOW.
4. **Test honesty: `test_three_turns_read_prior_not_stale` could be mutation-blind for finalize-on-read?** We determined it's not; the test would fail if finalize-on-read is removed. So okay.
5. **Potential off-by-one where turn N could read turn N's own directive if window opened early?** The call site is before `pre_llm_call`, so safe.
6. **No reentrant lock issues** but maybe a deadlock if `_bus_for` called from both consumer and produce path while lock held? No, `_bus_for` doesn't acquire lock.
7. **`_LAST_DIRECTIVE` not freed after `_budget_from_disk` sets it?** Actually `_budget_from_disk` does not set `_LAST_DIRECTIVE`; it returns a budget. So not a leak.
8. **`_bus_for` in `_budget_from_disk` may leave a bus cached but `_LAST_DIRECTIVE` empty, so next turn may rely on it but it's empty, leading to default. That's okay.
9. **The `_TEMPLATE_VALIDATED` cache is global, but `_ensure_template_valid` is called inside the lock, so fine.
10. **`_operator_budget` reads config multiple times?** It's memoized, so fine.

Now, find other subtle issues.

Look at `_resolve_bounded`: It calls `_ensure_template_valid()`, which does `_operator_budget()` to get budget for the probe. Under lock, that's fine. However, `_ensure_template_valid` only runs once; it uses the global `_TEMPLATE_VALIDATED`. If the template validation fails, it logs an error. The consumer then proceeds to close the window with its own `issue_policy` call (which is independent). So that's fine.

Now, check the `bounded_iterations` function itself: it checks `if not isinstance(default, int) or isinstance(default, bool): return default`. That means if default is a bool (True/
